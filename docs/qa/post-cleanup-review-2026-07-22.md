# DIVE 정비 후(S-058~S-070) 전수 재리뷰 (2026-07-22)

## 개요

- **대상**: spec 013 공개전환 정비(124건→13 Stage 처리)가 끝난 뒤의 `main`(`d7a5eaf`) 코드 전수 재리뷰.
- **기준선**: `docs/qa/public-release-code-review-2026-07-20.md`의 124건과 `specs/013-public-release-readiness/decisions.md`(D-013-01~16)의 의도적 보류·거부 항목은 baseline으로 제외.
- **방법**: finder 22(Stage별 회귀 10 + 신규/누락 영역 11 + verify:v4 감사 1) → dedup → 발견별 적대 검증(기각 기본값, P0/P1은 2차 독립 반박 재확인) → 커버리지 크리틱 → 갭 finder 6 → 2차 검증. 총 에이전트 122, read-only.
- **표준 게이트 실측(리뷰 시작 시점 main)**: cargo fmt/clippy(-D warnings, dev-mock+featureless)/test 전부 green, FE format/typecheck/lint/test:unit(580) green, **verify:v4 31/31 PASS**, version-sync 45 PASS.
- **결과: 생존 70건 — P0 2 · P1 11 · P2 57** (신규 13 · 지난 리뷰 누락 53 · 정비 회귀 4). 기각 2, 갭 라운드 확정 17.

> 최고 심각도 항목(P0 git-history, P0 verify-product-flow, P1 supersede 비대칭, P1 verify:v4 타우톨로지, P1 .wily 히스토리, P2 read_file .env 비대칭 등)은 루트가 git/gh/코드로 **직접 재확인**했다. 나머지는 워크플로 적대 검증(2차 독립 확인 포함) 결과다.

---

## P0 (2건)

### [P0] verify-product-flow hard-read internalized conference-poster doc — verify:v4 ENOENT on every clean checkout, blocking build.yml and release-gate Gate A (independently reproduced; fixed on main by 98c19db)
- **위치**: `dive/scripts/verify-product-flow.mjs`:107  · 유형: 신규(정비 산물)  · finder: `verify-v4-audit`
- **내용**: Finding accurate as filed against the audited range (a6bff3d..758d807): line 107's unguarded read of the internalized 04-Wily-Stage-실행계획.md ENOENT-crashes verify-product-flow on any clean checkout (reproduced via git-archive export, EXIT=1), failing verify:v4 in build.yml and release-gate Gate A and thereby blocking release.yml; the exists()-guarded evidence-template check at 108-110 would fail next. The a6bff3d "verify:v4 green" evidence was collected on the owner's machine where untracked local copies mask the miss — confirmed by the fix commit's own message. Already remediated on main by 98c19db (removes both conference-poster checks); clean export of current HEAD passes, so the only residual action is correcting the D-013-15 record's "all hard-reads fixed" claim.
- **증거**:
  ```
  verify-product-flow.mjs:107: const stagePlanDoc = read("docs/research/conference-poster/04-Wily-Stage-실행계획.md"); $ git cat-file -e HEAD:docs/research/conference-poster/04-Wily-... → NOT-IN-HEAD (only references.md tracked) fresh-clone sim (git archive HEAD): Error: ENOENT … 04-Wily-Stage-실행계획.md → verify-product-flow EXIT=1, verify-v4-all EXIT=1 .github/workflows/build.yml:43: - run: pnpm verify:v4   (on: push/PR → main) .github/workflows/release-gate.yml:57: pnpm verify:v4
  ```
- **검증**: a6bff3d:dive/scripts/verify-product-flow.mjs:107 read("docs/research/conference-poster/04-Wily-Stage-실행계획.md") is an unguarded readFileSync while git ls-tree shows only references.md tracked under that dir → ENOENT crash on every fresh checkout. build.yml:43 and release-gate.yml:57 both run pnpm verify:v4, and release.yml:114 requires an approved release-gate run, so CI was deterministically red and releases hard-blocked on the public repo. Not baseline: D-013-15 (decisions.md:219-222) claims these hard-reads "were fixed" — this is a missed case, and the 2026-07-20 review only touches conferen

### [P0] OMC purge never covered the committed .omc/ directory: 25 objects (private KACE interview playbook, owner telemetry with home/transcript paths and spend, project-memory with scrubbed projectRoot) are publicly served from airmang/DIVE main and all eight rc tags; D-013-14/D-013-15/D-013-16 "0 omc/omx reachable" verifications are object-level false
- **위치**: `specs/013-public-release-readiness/decisions.md`:203  · 유형: 신규(정비 산물)  · finder: `gap0-Git history / public`
- **내용**: Finder and first verifier are accurate in every particular I checked. Two sharpenings: (a) decisions.md:226 (D-013-15) is a THIRD false "0 omc/omx remain reachable from main" record, alongside :203 (D-013-14) and :155 (D-013-16) — all three passes re-asserted the claim without an object-level rev-list scan; (b) the miss mechanism is confirmed structurally: .omc/ is absent from the HEAD tree (git ls-tree origin/main .omc = empty), so every tree/working-tree-level check passed while history-reachable objects survived, and D-013-14's own text shows the filter-repo --invert-paths list contained only the two docs/research/omc\|omx-deep-interview files, never .omc/. The refs/pull/* caveat in D-013-14/15 does not cover this: it defers PR-ref content, and D-013-16 records the fresh repo never received PR refs — the exposure here rides refs/heads/main and refs/tags/*, exactly the refs that were pushed. Exact figures verified: 25 objects (14 blobs + 11 trees); playbook blob 426da9df = 13,092 B; telemetry total_cost_usd = 15.148514550000005; introducing commit d854dfcf... = d854fcf61d9f5ce47437334da22228d2d61d0e16 contained in all eight pushed tags. Fix: fourth filter-repo pass with .omc/ (and .omx/ if any history hits) in --invert-paths, force-push main, re-cut and force-push all eight rc tags, verify with `git rev-list --objects <each ref> \| grep '\.omc/'` = 0 on every ref, and amend D-013-14/15/16 to record the failed-then-repaired verification plus the corrected verification method (object-level rev-list, not tree-level).
- **증거**:
  ```
  $ git rev-list --objects origin/main \| grep -c '\.omc/'   -> 25 (same for v1.0.0-rc.9) $ git cat-file blob 02ca70d1 \| grep projectRoot   "projectRoot": "/Users/wilycastle/Code/projects/DIVE-2", $ git tag --contains d854fcf  -> v1.0.0-rc.2-internal … v1.0.0-rc.9 (all pushed tags) decisions.md:203 "…— 0 omc/omx objects and 0 PDFs remain reachable from main." hud-stdin-cache: transcript_path=/Users/wilycastle/.claude/…, total_cost_usd:15.14, rate_limits used 85%
  ```
- **검증**: Reproduced against the LIVE remote, not just local refs: `git ls-remote origin` shows refs/heads/main=d7a5eaf and refs/tags/v1.0.0-rc.9=9939bb87, identical to local tracking refs, and `gh repo view airmang/DIVE` returns visibility PUBLIC (pushedAt 2026-07-21). `git rev-list --objects origin/main \| grep -c '\.omc/'` = 25 (same for v1.0.0-rc.9); `git tag --contains d854fcf` = all eight pushed tags v1.0.0-rc.2-internal..rc.9; `git diff-tree d854fcf` confirms that commit introduced the .omc/ files, which were never in any S-070 --invert-paths list (the pass purged only docs/research/omc\|omx-deep


## P1 (11건)

### [P1] Checkpoint restore recreates every tracked entry as a fresh 0644 regular file — exec bits and symlinks recorded in the snapshot are silently destroyed, unrecoverably via the product
- **위치**: `dive/src-tauri/src/checkpoint/mod.rs`:799  · 유형: 지난 리뷰 누락  · finder: `fresh-rust-infra`
- **내용**: write_tree_to_disk (checkpoint/mod.rs:768-816) materializes every ObjectType::Blob tree entry via std::fs::write(&abs, blob.content()) (:799) without ever consulting entry.filemode(). Snapshots do record fidelity (probe-verified on macOS with git2 0.18.3: core.filemode=true in the bare sidecar repo; executables → 100755, symlinks → 120000 Blob entries whose blob content is the link target), and clear_tracked_worktree (:470-505) removes all tracked entries first, so restore always creates fresh umask-default files — there is no in-place-overwrite path that preserves modes. Result: +x silently lost (gradlew/mvnw/scripts fail with "permission denied") and symlinks become text files containing the target path (.venv/bin trees broken); no error surfaces because every write succeeds, and the auto-pre-restore backup (:287) restores through the same writer, so the corruption cannot be undone in-product. Fix must branch on entry.filemode(): FILEMODE_LINK → std::os::unix::fs::symlink(blob content as target); FILEMODE_BLOB_EXECUTABLE → set 0o755 (Unix-only, compiled out on Windows). Note the :778 kind filter cannot distinguish symlinks — 120000 entries report kind Blob.
- **증거**:
  ```
  match entry.to_object(repo) {     Ok(obj) => {         if let Some(blob) = obj.as_blob() {             if let Err(e) = std::fs::write(&abs, blob.content()) { // (write_tree_to_disk, lines 796-799 — entry.filemode() is never read anywhere in the file)
  ```
- **검증**: All refutation angles fail. (1) Snapshot records modes: commit_snapshot_opt (checkpoint/mod.rs:524-537) does index.add_all(["*"]) over the project workdir of a bare in-project sidecar repo; empirical probe on this macOS filesystem confirms bare-repo init sets core.filemode=true and an add over the worktree records 100755 for executables and 120000 for symlinks (git2 0.18 vendored libgit2 has identical lstat-based semantics). (2) The "overwrite preserves existing permissions" escape is dead: restore_checkpoint (mod.rs:294-295) calls clear_tracked_worktree first, which std::fs::remove_file's eve

### [P1] Observation draft text and evidence kind persist across step switches while the observe stage is active — stale step-A prose is one-click recordable as step B's S-029 evidence
- **위치**: `dive/src/components/product/VerificationCoachPanel.tsx`:92  · 유형: 지난 리뷰 누락  · finder: `fresh-fe-product`
- **내용**: Two precision corrections, defect otherwise as the first verifier stated. (a) The panel instance survives a step switch ONLY while "observe" is the active stepper stage at switch time — non-active stage content unmounts (VerificationReviewStepper.tsx:172-198), clearing the draft, so the strict path record→Next→decision→approve within step A does NOT leak. It is still the common case: no code auto-advances the stage after Record (no programmatic visitStage in StepDetailSlideIn), and line 239 keeps the textarea filled post-record, so mid-observation rail browsing and record→close→reopen-when-B-enters-review both carry step A's prose to step B, pre-filled with B's first criterion pre-selected. (b) The "zero actions on non-feasible steps" amplifier (StepDetailSlideIn.tsx:354-358) is the designed S-029 fallback for steps with no concrete check, not an independent defect — the defect is specifically that the typed text satisfying that fallback can be another step's stale prose. Same non-reset pattern in DecisionGate riskReason (DecisionGate.tsx:59) confirmed as a secondary instance. Fix: reset observationText/evidenceKind in the existing requestKey effect (VerificationCoachPanel.tsx:111-116), and key/reset DecisionGate's riskReason per step.
- **증거**:
  ```
  const [observationText, setObservationText] = useState(""); … // Re-seed the default selection (first criterion only) whenever the step // changes — … switching steps never leaves a stale cross-step selection checked. useEffect(() => {   … setSelectedCriterionIds(first ? [first.criterionId] : []); }, [requestKey]);   // observationText / evidenceKind NOT reset
  ```
- **검증**: Confirmed by direct code trace: VerificationCoachPanel.tsx:92-93 state is never reset on requestKey change (effect at 111-116 resets only selectedCriterionIds); VerificationReviewStepper.tsx:172-174+38-50 keeps the same "observe"-stage panel instance mounted across step switches (stable stage id, persistent activeStageId, no per-step key at StepDetailSlideIn.tsx:1140-1150); ProductShellLayout.tsx:77-87 (S-064 E2) keeps the slide-in mounted across close/reopen; useProductShellController.ts:360-378 makes the switch reachable in production; StepDetailSlideIn.tsx:354-358 (`!concreteVerificationFea

### [P1] Reattaching to a session mid-assistant-stream drops the entire in-flight assistant response from the visible transcript (recoverable only by re-entering the session after the turn completes)
- **위치**: `dive/src/hooks/chat-session-reducer.ts`:271  · 유형: 지난 리뷰 누락  · finder: `fresh-fe-features-stores`
- **내용**: Finder's mechanics verified exactly; two sharpenings. First, this is transcript-level loss, not data loss: finish_external_turn persists the assistant message (agent/mod.rs:656), so it reappears if the user leaves and re-enters after completion — but mid-stream the UI gives zero cue (no streaming indicator, since the remounted hook's runTerminal latch stays true because assistant_delta is not a RUN_START_EVENT_TYPE, and no stall timer arms), then shows only the 세션 종료 (turn_completed) system line with no reply, so a beginner will conclude the turn failed and re-send, burning a duplicate turn. Second, the minimal fix is to upsert on unmatched assistant_end (which carries the complete evt.content), mirroring the mergeMessagesById pattern already used by tool_call_start; optionally also upsert an assistant row on unmatched assistant_delta to restore live re-streaming. The tool-approval reattach already works via the pending_tool_calls replay (useChatSession.ts:541-554), confirming reattach is a designed flow and plain streaming is the one overlooked case.
- **증거**:
  ```
  case "assistant_end": {   const messages = prev.messages.map((m) =>     m.id === evt.id && m.kind === "assistant"       ? { ...m, content: evt.content, streaming: false }       : m,   );   // no match → final content dropped, nothing appended
  ```
- **검증**: chat-session-reducer.ts:262-294 — assistant_delta/assistant_end only .map() with no upsert (tool_call_start at :327 uses mergeMessagesById, so only the plain-stream reattach is broken). Reachable end-to-end: Sidebar.tsx:256 allows ungated session switch mid-stream; chat_send (ipc/chat.rs:602) runs run_supervised_turn to completion regardless, emitting fire-and-forget events (ipc/chat.rs:374) that are lost while unsubscribed; the live pi_sidecar path persists the assistant row only at end-of-turn via finish_external_turn → persist_assistant_message (agent/mod.rs:656), so the remount's message_l

### [P1] Internal agent-planning trace family .sisyphus/ (7 blobs, 159,109 B: Korean productization plans + Codex-QA prompt docs) reachable from the live public airmang/DIVE main and all 8 rc tags — missed by all three filter-repo purge passes and every D-013 verification
- **위치**: `.sisyphus/plans/dive-productization-v4.md`  · 유형: 지난 리뷰 누락  · finder: `gap0-Git history / public`
- **내용**: History-only objects under .sisyphus/ (plans/dive-productization-v4.md, plans/dive-ux-refactor-chat-first.md, plans/roadmap-redesign.md, drafts/ux-refactor.md, codex-qa/{prompt,scenarios,testids}.md; added 6c4264c/e43d456, tree-removed by merge fb13113) remain reachable from origin/main (= remote main d7a5eaf) and every rc tag on the fresh, already-public airmang/DIVE — 13 path-entries, 7 blobs, 159,109 bytes. They appear in no D-013 decision, no filter-repo path list, no verification scope (D-013-16 checked only PDFs/omc-omx/internalized-docs/refs-pull), and .gitignore has no .sisyphus entry. Sharpening vs the finder: the home-path element is pre-dispositioned (D-013-09 accepted home paths in history as non-sensitive), so the defect rests on the internal agent-planning + QA-prompt content class, which D-013-14/D-013-15 established as must-purge-from-history under the owner's minimal-public standard. Fix: fourth git filter-repo --invert-paths pass covering .sisyphus/ (bundleable with any .omc re-purge) + force-push of main and the 8 rc tags, plus adding .sisyphus/ to .gitignore.
- **증거**:
  ```
  $ git rev-list --objects origin/main \| grep -c '\.sisyphus/'  -> 13 (7 blobs, 159,109 B) $ git cat-file blob cc643e0f \| head -3 # DIVE 제품화 계획 v4 — 제품다움(Product Polish) 라운드 **작성**: 2026-05-05 **베이스**: `/Users/wilycastle/Code/projects/DIVE-2/` added 6c4264c…, tree-removed by fb13113; named in no D-013 decision or purge list
  ```
- **검증**: Reproduced read-only in /Users/wilycastle/Code/projects/DIVE-2: git rev-list --objects origin/main \| grep '\.sisyphus/' = 13 entries; the 7 blobs sum to 159,109 B (git cat-file -s); blob cc643e0f opens with '# DIVE 제품화 계획 v4' + owner home path; git ls-remote origin main = d7a5eaf = local origin/main so the objects are on the remote; gh api repos/airmang/DIVE → "visibility":"public" (exposure is live, not hypothetical); all rc tags reach 13 entries each; fb13113 tree has 0 sisyphus vs parent's .sisyphus (history-only, as claimed). Not BASELINE: zero mentions of sisyphus in docs/qa/public-relea

### [P1] ~690 KB of internal agent-orchestration material (.wily/ 176 blobs incl. engineering-audit handoff with home paths and third-party account details; codex_prompts/dive2_agent_handoff/PHASE*_HANDOFF/RALPH 52 blobs) is live-fetchable from the now-PUBLIC repo's history and rc tags — never included in any of the three filter-repo purge lists nor dispositioned in the decision log
- **위치**: `.wily/handoffs/legacy/dive-engineering-audit-report.md`  · 유형: 지난 리뷰 누락  · finder: `gap0-Git history / public`
- **내용**: Verified exact: 228 objects / 176 blobs / 384,945 B of .wily/ (roadmap-v2 phase/plan/verification archive, legacy handoffs incl. dive-engineering-audit-report.md with owner home paths, /tmp QA-home layout, and opencode zen free-tier account behavior) plus 52 blobs / ~300 KB of codex_prompts/ (36+INDEX, removed f992dd8), docs/dive2_agent_handoff/ (8 docs incl. 03_CLAUDE.md, removed 80b4b43), PHASE3-6_HANDOFF, and RALPH_PROMPT/README are reachable from origin/main and from the rewritten tags (v1.0.0-rc.9 carries all 228 .wily objects). Sharpening: the repo is ALREADY public (gh: visibility PUBLIC, pushed 2026-07-21, per D-013-16 fresh-repo flip), so this is a live exposure, not a pre-flip risk. None of the three filter-repo passes (D-013-13 PDFs/poster, D-013-14 omc/omx, D-013-15 internalized docs + only docs/superpowers/plans/ and root DIVE_NEXT/PROGRESS as old-path families) included these trees, and no decision-log entry dispositions them — yet the content class is exactly what D-013-15's minimal-public owner decision purged (internal agent-planning/supervision process material). Bonus inconsistency: live public doc docs/packaging-windows.md:94,185 still references .wily/phases/ paths that resolve in this public history. No secrets in the material (consistent with the review's clean history scan), so not P0; disposition (purge in the same pass as any .omc/.sisyphus re-purge, or explicit acceptance in the decision log) is the required fix.
- **증거**:
  ```
  $ git rev-list --objects origin/main \| grep -c '\.wily/'  -> 228 (176 blobs, 384,945 B) $ git cat-file blob 2f9d8c59 \| head -3 # DIVE Engineering Audit Report Date: 2026-05-17 Workspace: `/Users/wilycastle/Code/projects/DIVE-2` scaffold families (codex_prompts/, dive2_agent_handoff/, PHASE*_HANDOFF, RALPH_*): 52 blobs, 304,010 B
  ```
- **검증**: Reproduced read-only: `git rev-list --objects origin/main \| grep -c '\.wily/'` = 228 (176 blobs, 384,945 B — exact match); blob 2f9d8c59 is the engineering audit report containing /Users/wilycastle paths, /tmp/dive-qa-home layout, and opencode zen FreeUsageLimitError details as quoted; scaffold families = 52 blobs ~300 KB with removal commits f992dd8/80b4b43 confirmed; v1.0.0-rc.9 tag also reaches all 228 .wily objects; `gh repo view airmang/DIVE` = PUBLIC (2026-07-21). Not baseline: decisions.md D-013-13/14/15 enumerate purge scopes (PDFs/poster, omc/omx, internalized tracked docs + only doc

### [P1] Superseding a step that active steps depend on leaves dependents indefinitely blocked — remove has the dependent guard, supersede does not and nothing re-points dependents
- **위치**: `dive/src-tauri/src/ipc/workspace_plan/plan_steps.rs`:636  · 유형: 지난 리뷰 누락  · finder: `gap1-ipc/workspace_plan/*`
- **내용**: workspace_plan_supersede_step_impl (plan_steps.rs:591) marks the target 'superseded' without the validate_no_active_dependents check that remove performs (plan_steps.rs:527) and without rewriting dependents to the replacement's new step_id (append_step_within_tx always allocates a fresh id, step.rs:140). The superseded step drops out of list_active_by_plan, so done_step_ids can never contain its id; blocked_dependency_ids therefore reports every dependent blocked and roadmap_step_open_impl (roadmap.rs:40-61) refuses to open it, with the dashboard offering no recovery. Live path: chat router proposes SupersedeStep for any active step (plan_routing.rs:175-192; resolve_active_step_ref checks only status=='active') and usePlanChatRouting.ts:180 applies it on modal confirm. Two precision notes on the finder's 'permanently': (1) a dependent already opened before the supersede bypasses the block via the existing-mapping early return (roadmap.rs:32-34); (2) the theoretical escape is superseding the dependent itself with dropped deps — possible but suggested nowhere, so for the common un-opened case it is a dead-end in practice for the beginner audience. The dangling dep also never surfaces later: step_dao::validate_dependencies validates against list_by_plan (all statuses), so subsequent appends pass silently. Untested asymmetry: remove_step_with_active_dependent_is_rejected exists (workspace_plan_ipc.rs:2445) but no supersede-with-dependent test. Fix: add the same dependent guard to supersede, or rewrite dependents' dependency arrays to the replacement's step_id inside the same transaction (the latter matches supersede's replace semantics).
- **증거**:
  ```
  if target.status != "active" {     return Err(format!("step {} is not active", target.step_id)); } validate_draft_dependencies(&tx, plan_id, &replacement.dependencies)?; // (no validate_no_active_dependents — contrast remove_step_impl line 527)
  ```
- **검증**: All load-bearing claims verified against code: remove has the dependent guard (plan_steps.rs:527 validate_no_active_dependents) while supersede does not (plan_steps.rs:591-715; line 636 validates only the replacement's own deps) and nothing re-points dependents (superseded_by_step_id is persisted/checkpointed/logged but never consulted for dependency resolution — grep across src). done_step_ids (dashboard.rs:291-305) maps mappings through the active-only step list (list_active_by_plan, step.rs:89-91) at every caller (roadmap.rs:36, dashboard.rs:118, plan_steps.rs:1038), so a superseded step_id

### [P1] Deleting a step's session leaves a zombie NULL-session StepSessionMapping that roadmap_step_open returns as-is — step start silently no-ops and step chat is rejected forever, with no repair path
- **위치**: `dive/src-tauri/src/ipc/workspace_plan/roadmap.rs`:32  · 유형: 지난 리뷰 누락  · finder: `gap2-DAO layer + models.r`
- **내용**: Deleting a plan step's session from the sidebar (confirm-guarded button, session-delete testid) leaves a zombie StepSessionMapping: PRAGMA foreign_keys=ON makes schema.rs:171's ON DELETE SET NULL fire, and the step's Card cascade-deletes (schema.rs:33), so the mapping survives with session_id=NULL, card_id=NULL, status still 'in_progress'. roadmap_step_open_impl (roadmap.rs:32-34) early-returns that zombie instead of minting a fresh session — the sole production mapping-insert (roadmap.rs:104; the other two insert sites are #[cfg(test)]-only) is in the branch it skips, and roadmap_step_update_state_impl copies the NULL session_id forward. Both FE start-step consumers guard on `mapping.session_id !== null` and silently do nothing (ProductShellLayout.tsx:110, PlanDashboardPanel.tsx:139 — openStep resolves, so no error path fires), and chat's load_active_step_context rejects every session for that step forever. Checkpoint restore is not a repair route (mapping cleanup is `WHERE session_id = ?`, never matching NULL; the deleted session's checkpoints cascade away). Recovery requires approving an entirely new plan or manual DB surgery. Fix direction as stated: treat a NULL-session mapping as absent in roadmap_step_open (re-mint session+card, preserving verification fields), or clean up/reset the mapping on session delete.
- **증거**:
  ```
  schema.rs:171  session_id INTEGER REFERENCES Session(id) ON DELETE SET NULL roadmap.rs:32  if let Some(existing) = mapping_dao::get_by_step(conn, step_id)… { roadmap.rs:33      return Ok(existing); session.rs:168 session_dao::delete(db.conn(), session_id)…  // no mapping cleanup ProductShellLayout.tsx:110  if (mapping.session_id !== null) { …focus/select… }  // else: silent no-op
  ```
- **검증**: Every link in the chain checks out against the code. (1) PRAGMA foreign_keys=ON is set (db/mod.rs:110), so StepSessionMapping.session_id's ON DELETE SET NULL (schema.rs:171) actually fires; the Card cascades away too (Card.session_id NOT NULL ON DELETE CASCADE, schema.rs:33), leaving a mapping with session_id=NULL, card_id=NULL, status 'in_progress'. (2) session_delete (ipc/session.rs:159-169) deletes only the Session row — no mapping cleanup. (3) The trigger is an ordinary UI action: roadmap_step_open_impl creates a normal project Session titled after the step (roadmap.rs:64-73), session_list

### [P1] No concurrency guard on checkpoint restore at any layer — a second restore or save-point during an in-flight restore interleaves lock-free worktree rewrites and corrupts the student project
- **위치**: `dive/src/components/product/RecoveryPanel.tsx`:301  · 유형: 지난 리뷰 누락  · finder: `gap3-Unread FE component `
- **내용**: All finder claims verified accurate, with two sharpenings. (1) The UI failure is worse than 'other buttons stay enabled': after the user confirms a restore, the confirm inline unmounts immediately (setConfirmRestoreId(null), RecoveryPanel.tsx:232) and the 'Restoring…' label lives only inside that now-closed inline — the panel shows no busy indication at all until the success toast, so a beginner perceiving 'nothing happened' naturally clicks another Undo. (2) The interleaving is self-compounding: restore B's own 'auto-pre-restore' safety checkpoint (mod.rs:287) snapshots the half-cleared/half-written tree left mid-flight by restore A, so B's designated recovery point is itself garbage; B's clear_tracked_worktree pass (driven by the sidecar HEAD that A's pre-restore commit just advanced) then deletes files A is concurrently writing; the final worktree is a mix of trees A and B with HEAD (mod.rs:297) matching neither. handleManualCheckpoint (useProductRecovery.ts:95) is equally unguarded, so 'Save point' mid-restore commits a half-cleared tree as a manual checkpoint. Recoverability survives via restore A's intact pre-restore snapshot, but only for a user who knows to restore again — the target audience (beginner students) does not. Fix direction: a global restore-in-flight guard (disable all restore/save/refresh actions while restoringCheckpointId !== null) plus a backend mutex or busy-rejection around CheckpointEngine worktree mutation.
- **증거**:
  ```
  RecoveryPanel.tsx:301  disabled={restoringCheckpointId === item.id} useProductRecovery.ts:122  setRestoringCheckpointId(checkpointId); // no early-return if already restoring checkpoint/mod.rs:295-296         self.clear_tracked_worktree(&repo)?;         write_tree_to_disk(&repo, &tree, &self.project_root)?;  // no fs-level lock held
  ```
- **검증**: No concurrency guard exists at any layer, verified end-to-end: RecoveryPanel.tsx:301 and :229 disable only the button matching restoringCheckpointId (all other Undo buttons and Save point stay enabled, and the confirm inline closes instantly on click, leaving zero in-flight indication); useProductRecovery.ts:120-141 has no busy early-return; lib/tauri.ts:20 is a raw core.invoke passthrough; ipc/checkpoint.rs:76 checkpoint_restore is an async Tauri command (default multi-threaded tokio runtime, no override in src-tauri) so two invocations run in parallel; checkpoint/mod.rs:273-302 holds the db 

### [P1] Closed-but-mounted StepDetailSlideIn still fires verification-coach LLM calls and writes coach REQUESTED/EVALUATED ledger events when the stepper's active stage was 'observe' at close (S-064 I1 gate missed the coach effect)
- **위치**: `dive/src/components/product/VerificationCoachPanel.tsx`:130  · 유형: 회귀(정비가 유발)  · finder: `s064-correctness`
- **내용**: S-064 (ffb35ac) latched StepDetailSlideIn mounted after first open (E2 fix) and gated the supervisor-eval effect on `open` (I1), but VerificationCoachPanel — mounted as the stepper's 'observe' stage content — has an ungated useEffect([nonce, requestKey]) calling generateVerificationCoachGuide → IPC verification_coach_generate → sidecar LLM, with the Rust command appending VERIFICATION_COACH_REQUESTED unconditionally before any runtime check and VERIFICATION_COACH_EVALUATED after, into the supervision-evidence EventLog. Two precision corrections to the finder: (a) exposure requires 'observe' to be the CURRENTLY ACTIVE stepper stage at close (persisted activeStageId, never reset while the panel is closed) — not merely 'ever visited'; advancing past observe before closing unmounts the coach; (b) if the card leaves 'review' state during the fix chat, isReview flips false and the request nulls out, so the live triggers are a verify run completing after close (test_result/intent_match change) or agent file edits while the card remains in review. Closing does not clear the selected step (controller flips only the boolean) and changedFiles/verifyLog keep flowing as live props; requestKey retains evidence.changedFiles/testResult/aiClaimedDone (only guideVersion/priorObservations stripped), so each such background update fires an unrequested LLM call plus two ledger appends the student never triggered — the exact class the I1 comment in the same file names and claims to prevent. Fix: pass `open` down and gate the coach effect (mirror I1: cancel in-flight on close, keep response state for reopen), or unmount/reset stage content while !open.
- **증거**:
  ```
  VerificationCoachPanel.tsx:129-133 (no open gate):     setLoading(true);     void generateVerificationCoachGuide({ ... })   }, [nonce, requestKey]); StepDetailSlideIn.tsx:915 renders it inside reviewStages 'observe' content; VerificationReviewStepper.tsx:172 mounts active stage content whenever the (closed) panel is mounted
  ```
- **검증**: Full chain verified read-only. (1) Mount latch: ProductShellLayout.tsx:84-87,214-216 keeps StepDetailSlideIn mounted forever after first open; closed state is only translate-x-full + inert (StepDetailSlideIn.tsx:1060,1073), which blocks interaction but not rendering/effects. (2) VerificationReviewStepper renders in the body regardless of open (StepDetailSlideIn.tsx:1140) and mounts the active stage's content (VerificationReviewStepper.tsx:172-174); activeStageId is component state never reset on close, so a student who closed while on "observe" keeps VerificationCoachPanel mounted (StepDetailS

### [P1] S-029 evidence latches never invalidate across revision rounds after the E2 keep-mounted fix — round-2 approval of an unseen diff is pre-satisfied by round-1 evidence, and approval provenance stamps stale observation ids as backing it
- **위치**: `dive/src/components/product/StepDetailSlideIn.tsx`:182  · 유형: 회귀(정비가 유발)  · finder: `s064-correctness`
- **내용**: Finder mechanics verified accurate. Two sharpenings: (a) beyond gate bypass, buildApprovalProvenance (useProductShellController.ts:409-439) records the round-2 approval with round-1 observationIds/manualChecks and acceptanceCriterionConfirmed=true, so the provenance/EventLog ledger — the research over-trust anti-metric — misreports stale evidence as covering the new diff, meaning even the intended "blind approval is recorded, not prevented" posture is defeated; (b) approval is possible while the fresh supervisor eval for the new changedFiles is still in flight (provocationCards=[] during pending, review-card-pending stage is display-only), so even a high-risk round-2 diff cannot block in that window. Bounded: latches clear on ProductShellLayout unmount (project/session switch, restart), but the mainline request-changes→revise→re-review loop never leaves the mount. Fix direction: invalidate the per-step latches (diffViewed/previewOpened/appOpened/manualObservations) on changedFiles or verify-evidence change for that step, or on the revision_requested transition.
- **증거**:
  ```
  StepDetailSlideIn.tsx:182-188 — per-step latches, no invalidation path:   const [diffViewedStepIds, setDiffViewedStepIds] = useState<Set<number>>(() => new Set());   const [manualObservationsByStep, setManualObservationsByStep] = useState<...>(() => new Map()); decisionGatePolicy.ts:170: canApproveDirectly: reasons.length === 0 && !canDeferVerification useProductShellController.ts:475: dialogs.setStepDetailOpen(false);  // after every decision
  ```
- **검증**: All latches are add-only: StepDetailSlideIn.tsx:767/781/789/920 are the only writes to diffViewedStepIds/previewOpenedStepIds/appOpenedStepIds/manualObservationsByStep; only criterionEvidenceRef resets, and only on step-id change (213-215). Post-ffb35ac the panel never remounts (ProductShellLayout.tsx:84-87, 214-217 — everOpened latch, no key prop), whereas pre-fix (`git show ffb35ac~1`) it was `open ? <panel/> : null` and the controller force-closes after every decision (useProductShellController.ts:455/475/496), so each revision round previously restarted from wiped state. In round 2 the sta

### [P1] a6bff3d FE repoint drops the composition-edge guarantee: orphaning useShellMenus/usePlanChatRouting/ConversationSurfaces from their primary keeps every static verifier and the test suite green
- **위치**: `dive/scripts/verify-product-flow.mjs`:42  · 유형: 회귀(정비가 유발)  · finder: `verify-v4-audit`
- **내용**: The finding is substantively correct with two sharpenings. First, no mutation is needed: for each affected check every required needle is already satisfied by sibling files alone (requestPlanRouteConfirmation has zero occurrences in the controller; the three verification-honesty needles live only in StepDetailSections.tsx; PlanDraftRecoveryScreen/PlanDraftApprovalScreen needles are satisfied by ConversationSurfaces.tsx), so no compiling controller-side deletion can ever fail them. Second — correction to both the finder and the first verifier — the StepDetailSlideIn→StepDetailSections edge is NOT fully unguarded: StepDetailSlideIn.test.tsx renders the real StepDetailSections (it mocks only provocation/verification-coach) and asserts testids defined in Sections (step-detail-verification-focus:96, step-detail-evidence-chips:139), so wholesale orphaning of Sections fails vitest; only the three honesty needles themselves lack test assertions. The genuinely zero-coverage escapes are: controller→useShellMenus (track-d "subscribes to menu events"/"handles open project" and track-e help-tutorial now grep useShellMenus.ts alone; bare call at controller:501; no test references useMenuEvents) — native menu events die with all gates green; controller→usePlanChatRouting (product-flow "approved-plan chat routing"); and PlanDraftRecoveryScreen/ApprovalScreen mounting via ConversationSurfaces (verify-audit-fixes:84, verify-quality-followup:104-105; screen unit tests test the screens, not their mounting). Rust-side repoints are correctly exempt (mod tree + compiler pin the split files). Fix as proposed: additionally assert each primary contains its sibling's import/call (e.g. useProductShellController.ts includes "useShellMenus(" and "./usePlanChatRouting"; ConversationPanel/controller includes ConversationSurfaces; StepDetailSlideIn.tsx includes "<StepDetailSections"), restoring pre-S-068-split gate strength; also correct or annotate the D-013-15 "does not weaken" record.
- **증거**:
  ```
  M1: useShellMenus import+call removed from useProductShellController.ts → track-d EXIT=0, track-e EXIT=0, product-flow EXIT=0, production-wire EXIT=0 M2: usePlanChatRouting mentions left in controller: 0; requestPlanRouteConfirmation: 0 → PASS approved-plan chat routing asks before adding new plan steps (product-flow EXIT=0) track-d.mjs:100 now reads only useShellMenus.ts for /useMenuEvents\(/ (was useProductShellController.ts at a6bff3d^)
  ```
- **검증**: Reproduced read-only. (1) Not baseline: docs/qa/public-release-code-review-2026-07-20.md never mentions the verify scripts; decisions.md D-013-15 (lines 228-234) records the repoint as "no check deleted, no includesAll loosened" with no composition-edge caveat — this finding falsifies that recorded claim for the FE side. (2) The a6bff3d diff confirms verify-track-d.mjs:100,105 and verify-track-e.mjs:76 now read useShellMenus.ts INSTEAD of useProductShellController.ts (pre-repoint the code was inline in the grepped controller, so presence in the mounted unit was pinned; post-repoint it is not).


## P2 (57건)

### [P2] THIRD-PARTY-NOTICES omits bundled OFL font IBM Plex Sans KR (276 woff2 in dist, no license copy) and the sidecar's redistributed Node.js runtime + two MIT pi SDK packages whose license headers esbuild strips
- **위치**: `THIRD-PARTY-NOTICES.md`:1  · 유형: 지난 리뷰 누락  · finder: `s058-59-70-hygiene`
- **내용**: The finder was accurate; two sharpenings: (1) build-sidecar.mjs:272 sets esbuild legalComments:"none", so the shipped sidecar affirmatively strips the MIT license comments from @earendil-works/pi-ai and pi-coding-agent rather than merely omitting a notices entry — strengthening the MIT-attribution gap. (2) Exposure timing: per D-013-16 the installers currently live only on the renamed private archive, so no public artifact redistributes the fonts/runtime today; the public repo's exposure is a THIRD-PARTY-NOTICES.md that claims to index bundled third-party assets while covering only 2 of 3 bundled OFL font families and none of the runtime/SDK redistribution that any recreated public release (which D-013-16 explicitly contemplates) will carry. Fix is cheap: add IBM Plex Sans KR (OFL-1.1, node_modules/@fontsource/ibm-plex-sans-kr/LICENSE), Node.js runtime (its aggregate LICENSE), and the two MIT pi SDK packages to THIRD-PARTY-NOTICES.md, and ensure the license texts accompany built artifacts.
- **증거**:
  ```
  main.tsx:6-8: import "@fontsource/ibm-plex-sans-kr/400.css"; (+500,600) @fontsource/ibm-plex-sans-kr -> OFL-1.1; @earendil-works/pi-ai -> MIT; pi-coding-agent -> MIT build-sidecar.mjs: downloads nodejs.org dist, esbuild-bundles sidecar+SDK, injects SEA blob tauri.conf.json:33: "externalBin": ["binaries/dive-pi-sidecar"] THIRD-PARTY-NOTICES.md: "This project bundles third-party font assets under dive/src/assets/fonts/"
  ```
- **검증**: All evidence checks out and no guard exists: dive/src/main.tsx:6-8 imports @fontsource/ibm-plex-sans-kr (installed 5.2.8, license OFL-1.1) and the built dive/dist/assets contains 276 ibm-plex-sans-kr woff/woff2 files with zero license files anywhere in dist (tauri.conf.json:10 frontendDist=../dist ships them in installers); tauri.conf.json:33 externalBin ships dive-pi-sidecar, which build-sidecar.mjs constructs from the official nodejs.org Node binary plus an esbuild bundle of @earendil-works/pi-ai and pi-coding-agent (both MIT 0.80.6) with legalComments:"none" (build-sidecar.mjs:272) strippin

### [P2] Effect cleanup during the pending api.listen await leaks the chat event listener and its ever-growing bufferedEvents array
- **위치**: `dive/src/hooks/useChatSession.ts`:530  · 유형: 지난 리뷰 누락  · finder: `fresh-fe-features-stores`
- **내용**: Finding is accurate as stated. One sharpening: the leaked closure's callback returns after pushing to bufferedEvents (historyLoaded is permanently false, so setState/applyEventSideEffects are never reached), meaning this is a pure resource/memory leak — one live Tauri IPC listener plus an unbounded array that grows on every subsequent event in that session's channel — with no state corruption or duplicated side effects. That confines it to P2. The catch path (L568) has the same guard-return-without-unlisten as L545. Fix: when the resolved unlisten arrives and `cancelled` is already true, call it immediately (and likewise at both cancelled-guard returns after the listen assignment).
- **증거**:
  ```
  unsub = await api.listen<ChatEventPayload>(`chat://event/${sessionId}`, ...);  // L530 ... if (cancelled) return;   // L545 — returns without unsub() ... return () => { cancelled = true; clearStallTimer(); if (unsub) unsub(); };  // L578-582
  ```
- **검증**: useChatSession.ts:404/530/578-582 — `unsub` is assigned only after `await api.listen(...)` (a real @tauri-apps/api/event IPC round-trip, lib/tauri.ts:35) resolves; cleanup runs `if (unsub) unsub()`, so cleanup during the pending-listen window sees null, and when listen resolves both the success path (L545 `if (cancelled) return;`) and catch path (L568) return without unlistening and before `historyLoaded = true` (L546/L569). Grep confirms no other unsub/unlisten in the file; sessionId is in the effect deps so rapid session switching makes the window reachable. git show 8c895a2~1 confirms the i

### [P2] Sidebar project/session actions and settings provider-disconnect are silent unhandled rejections — S-062's runUserAction covered five sites but missed these seven void-ed handlers
- **위치**: `dive/src/components/shell/Sidebar.tsx`:131  · 유형: 지난 리뷰 누락  · finder: `fresh-fe-shell-pages`
- **내용**: Every Sidebar mutation handler (selectProject clicks at Sidebar.tsx:131/192, handleNewSession:58, handleDeleteProject:63, handleArchiveProject:70, handleUnarchiveProject:74, handleDeleteSession:78) and settings handleDisconnect (settings.tsx:277-284, void-ed at 556) awaits a store action that rethrows via runStoreAction into a void-ed promise with no catch, no toast, and no rendered error field. Reachable failure: project folder moved/deleted in Finder → project_select returns Err (project.rs:222-223) → sidebar click does nothing on screen; a failed confirmed disconnect leaves the provider rendered as connected. The startup loadAll path handles the same project_select error explicitly (project-session.ts:216-229), confirming the mid-session paths were overlooked, and handleCodexDisconnect try/catches its IPC while the plain disconnect path does not. Fix is mechanical: route these handlers through the existing lib/runUserAction + toast pattern from S-062.
- **증거**:
  ```
  Sidebar.tsx:131  onClick={() => void selectProject(p.id)} Sidebar.tsx:66   await deleteProject(id, false);   // handler void-ed at 155 settings.tsx:283 await disconnectProvider(row.id); // void-ed at 556 project-session.ts:181-182  } catch (err) { set({ error: errorMessage(err) }); project-session.ts:183      throw err;  // rethrown into a void-ed handler
  ```
- **검증**: Verified in code: runStoreAction (dive/src/stores/project-session.ts:176-183) rethrows after setting the store error field, which has zero readers (only .error hit in src/ is main.tsx ErrorBoundary local state), and there is no unhandledrejection handler in dive/src. All cited actions wrap runStoreAction (selectProject:477, deleteProject:382, archive/unarchive:433/452, createSession:524, deleteSession:610, disconnectProvider:699) and every cited handler discards the rejection via void with no catch (Sidebar.tsx:131/145/155/192/207/217/238/273; settings.tsx:283 void-ed at 556). The flagship sce

### [P2] Seven registered IPC commands (ai_assist_cards, card_list, codex_oauth_refresh, mcp_server_list_tools, mcp_server_set_enabled, preview_start, project_get) have zero renderer callers and were never dispositioned by the S-060 dead-IPC sweep or any D-013 decision
- **위치**: `dive/src-tauri/src/lib.rs`:94  · 유형: 지난 리뷰 누락  · finder: `s060-rust-deadcode`
- **내용**: Each of the seven is registered in generate_handler! (lib.rs:84,94,106,126,135,139,141) but has zero invoke() references in FE, scripts, or e2e, and no dynamic-string invoke exists in dive/src. Each belongs to a command family whose siblings the FE does call, making them conspicuous orphans to any external reviewer running the same cross-check. Pre-existing at e533a2b (not a regression). Baseline C4/D-013-05 dispositioned only the four commands the review named, and D-013-06/D-013-12 explicitly keep three others; these seven were never examined. None accepts secrets and none has internal Rust callers, so this is undispositioned dead renderer-facing surface: delete the registrations+fns or record a keep-decision in the D-013 log.
- **증거**:
  ```
  $ grep -rl 'ai_assist_cards\\|card_list\\|codex_oauth_refresh\\|mcp_server_list_tools\\|mcp_server_set_enabled\\|preview_start\\|project_get' dive --include='*.ts*' \| grep -v src-tauri (no output — zero JS/TS references; same result at e533a2b) lib.rs:94 `ipc::ai_assist_cards,` … :126 `ipc::preview_start,` all still registered.
  ```
- **검증**: All seven commands are still registered (dive/src-tauri/src/lib.rs:84 card_list, :94 ai_assist_cards, :106 project_get, :126 preview_start, :135 codex_oauth_refresh, :139 mcp_server_set_enabled, :141 mcp_server_list_tools) and grep over all .ts/.tsx/.js/.mjs in dive/src, dive/scripts, dive/e2e finds zero references; control greps find live siblings (project_list, card_tool_call_stats), and no non-literal invoke() first-arg exists anywhere in dive/src, so no dynamic dispatch can reach them. The FE actively invokes sibling commands in every family (preview_checked/preview_open, mcp_server_list/a

### [P2] Five backend events (checkpoint_created, verify_started, verify_done, codex://oauth-complete, codex://oauth-error) are emitted with no listener anywhere
- **위치**: `dive/src-tauri/src/ipc/cards.rs`:586  · 유형: 지난 리뷰 누락  · finder: `s060-rust-deadcode`
- **내용**: Finder was accurate; one sharpening: docs/archive/legacy-specs/DIVE_PLAN.md:240-254 shows these events were designed for FE subscription hooks (useCheckpointFeed, useVerifyStatus) that were never built — the shipped FE went with invoke return values and polling instead, so the emits are vestiges of an abandoned legacy plan, not intentional observational wiring. All five are `let _ = app.emit(...)` fire-and-forget, so runtime-harmless; the cost is payload serialization for nobody and a misleading signal to maintainers that the UI reacts. Fix is either deleting the five emits or a one-line comment marking them unsubscribed-by-design.
- **증거**:
  ```
  cards.rs:586 `"checkpoint_created",` :871 `"verify_started",` :925 `"verify_done",` codex_oauth.rs:334 `app.emit("codex://oauth-complete", &status);` $ git grep -F 'verify_done' HEAD -- dive/src/  → (no output); same for all five names at HEAD and e533a2b
  ```
- **검증**: All five emits verified with zero listeners repo-wide: emit sites at dive/src-tauri/src/ipc/cards.rs:586 (checkpoint_created), :871 (verify_started), :925 (verify_done) and dive/src-tauri/src/ipc/codex_oauth.rs:334/351 (codex://oauth-complete/-error); the complete FE Tauri-event listener surface is exactly three (dive/src/hooks/useChatSession.ts:530 chat://event/{sessionId}, dive/src/App.tsx:135 provider://changed, dive/src/lib/menu-events.ts:35 menu ids), no Rust-side .listen/listen_global exists, and a full-repo grep (excluding .git/target/node_modules) finds the five names only at emit site

### [P2] env-dump blocklist misses `export -p`/`declare -p`/`typeset -p` (shell path) and `node -p process.env` (argv path), which also dump exported vars to LLM output
- **위치**: `dive/src-tauri/src/tools/guard.rs`:167  · 유형: 지난 리뷰 누락  · finder: `s063-security`
- **내용**: Reproduced against the live regexes (guard.rs:166-167 env-dump, :136 interp): `export -p`, `declare -p`, `typeset -p`, `node -p process.env`, and `node --print process.env` all return no match and pass both classify_bash_command and assess_terminal_script. Reachability differs by tool: in run_terminal_script (`sh -c`) the three shell builtins execute and dump exported/declared variables; in run_process (structured argv) export/declare/typeset fail to spawn (no binary), but `{"command":"node","args":["-p","process.env"]}` runs because node is not a shell executable/wrapper and the interp rule anchors only on -c/-e/--eval, not -p/--print. The interp rule at guard.rs:136 catches `node -e`/`node --eval` but not `node -p`/`node --print` — a direct sibling miss. Severity is bounded by scrub_sensitive_env (process_exec.rs:62), which strips credential-named vars (API_KEY/TOKEN/SECRET/PASSWORD/etc.) from the child before spawn, so only secrets in non-standard-named exported vars (e.g. `MY_DB_PW`, which `is_sensitive_env_key` does not match) leak. Defense-in-depth completeness gap, not a common-case credential leak.
- **증거**:
  ```
  // env-dump regex only anchors env\|printenv\|set: (?:(?:env\|printenv)(?:\s\|$)\|set\s*(?:$\|[;&\|\r\n])) // export -p / declare -p / node -p process.env match nothing here and pass classify_bash_command
  ```
- **검증**: CONFIRMED, not baseline: node -e/node --eval-style tests in guard.rs prove the interp rule ignores `-p`/`--print`; a node one-liner reproduces false for all four forms against the env-dump, interp, dotenv, and echo-secret rules. Reachable in production — run_terminal_script executes `export -p`/`declare -p`/`typeset -p` via `sh -c` (terminal_script.rs:127-133), and run_process spawns `node -p process.env` (run_process.rs:95-97; node absent from SHELL_EXECUTABLES/EXEC_WRAPPERS at :134-158). Baseline F1 (public-release-code-review-2026-07-20.md:93) named only `{"command":"env"}`; D-013-08 (decis

### [P2] AgentOutcome is a dead pub struct that survived every dead-code stage
- **위치**: `dive/src-tauri/src/agent/mod.rs`:209  · 유형: 지난 리뷰 누락  · finder: `s065-pipeline`
- **내용**: pub struct AgentOutcome (dive/src-tauri/src/agent/mod.rs:209-212) is dead from birth: introduced in 72b09b1 with zero references and never used since (git -S shows no other commit; current repo-wide occurrence count is 1). It escaped the S-060 allow(dead_code) removal because pub items reachable from the lib crate root (lib.rs:3 pub mod agent; crate-type includes rlib/cdylib) are exempt from the dead_code lint, so neither clippy -D warnings CI nor the baseline C1 --force-warn dead_code census could see it, and no baseline item or D-013 decision names it. Fix is a 4-line deletion.
- **증거**:
  ```
  $ grep -rn "AgentOutcome" dive --include="*.rs" --include="*.ts*" dive/src-tauri/src/agent/mod.rs:209:pub struct AgentOutcome { $ git log --oneline -S "AgentOutcome" --all -- dive/src-tauri 72b09b1 feat(agent): add Agent Loop with tool registry, permissions, IPC (task 2-3)
  ```
- **검증**: dive/src-tauri/src/agent/mod.rs:209-212 defines `pub struct AgentOutcome { events, final_reason }` and a repo-wide grep (all file types, excluding .git/node_modules/target) finds exactly 1 occurrence — the definition. git log -S shows only commit 72b09b1 ever touched it, and that commit contains exactly 1 occurrence, so it was never referenced from birth. The exemption mechanism checks out: lib.rs:3 `pub mod agent` in a crate with crate-type ["staticlib","cdylib","rlib"] makes the pub struct externally visible, so rustc's dead_code lint (and C1's `--force-warn dead_code` census of 10 warnings)

### [P2] agent/mod.rs module headline describes the cfg-gated test-only AgentLoop::run as the module's turn loop — contradicting its own line-35 import comment and the shipped build
- **위치**: `dive/src-tauri/src/agent/mod.rs`:3  · 유형: 지난 리뷰 누락  · finder: `s065-pipeline`
- **내용**: The finder was correct but understated the contradiction: it is internal to the first 40 lines of the file. mod.rs:3-7 says "AgentLoop::run drives a single user turn...", yet the import comment at mod.rs:35-38 (added by the same S-065 commit 9803e87) already calls run "the in-process run/stream_assistant test harness, which is gated to test / dev-mock builds," and the fn doc at mod.rs:252-257 repeats that it is not part of the shipped surface. In a feature-less production build the symbol the module doc describes does not exist at all; the actual production entry points are begin_external_turn (mod.rs:560) and execute_supervised_tool_call (mod.rs:688) via pi_sidecar. Fix is a 5-line rewrite of the //! header to describe the supervised external-turn surface and demote run to a one-line test-harness mention. Doc-only; no behavioral consequence.
- **증거**:
  ```
  mod.rs:3  //! `AgentLoop::run` drives a single user turn: records the user message, mod.rs:4  //! streams the assistant reply, intercepts tool calls through the ... mod.rs:252 /// In-process, single-process turn loop used only as a test harness: ... mod.rs:258 #[cfg(any(test, feature = "dev-mock"))]
  ```
- **검증**: All quoted evidence checks out: mod.rs:3-7 module doc unqualifiedly presents AgentLoop::run as the module's turn loop, while 9803e87 (S-065) gated run behind #[cfg(any(test, feature = "dev-mock"))] (mod.rs:258) with a fn doc (mod.rs:252-257) saying it is a test harness "not part of the shipped surface"; the commit's diff starts at line 23, so the module doc was never updated. Production surface is begin_external_turn (mod.rs:560) + execute_supervised_tool_call (mod.rs:688), called from pi_sidecar.rs:543/725. Not baseline: review items C3/D2 are the code findings S-065 resolved, and specs/013-p

### [P2] ensure_provider_runtime installs stale hydrate result without CAS re-check, silently reversing concurrent provider connect/select/disconnect; no single-flight so parallel callers multiply keychain prompts
- **위치**: `dive/src-tauri/src/ipc/state.rs`:313  · 유형: 지난 리뷰 누락  · finder: `fresh-rust-ipc`
- **내용**: ensure_provider_runtime (state.rs:261-316) unconditionally swaps in a possibly-stale hydrate result: when the runtime is None it spawns a blocking hydrate (DB provider_dao::list + per-row keyring reads, up to 120s under the acknowledged macOS keychain-prompt scenario) and on completion calls swap_runtime(next) at state.rs:313 without re-checking under the write lock that the runtime is still None. Any concurrent user action that swapped the runtime meanwhile — provider_connect (provider.rs:200), provider_select (provider.rs:457), provider_disconnect (provider.rs:571) — is silently overwritten. Because provider selection is never persisted and hydrate resolves the last-created credentialed row (rows.into_iter().rev(), state.rs:414), the clobber does not self-correct: with providers A(older)/B(newer) connected, selecting A then having a stale hydrate land deterministically flips the runtime back to B until the user re-selects; a connect whose row post-dates the hydrate's DB snapshot is likewise clobbered by the previous newest row, and a stale hydrate that read keys before a disconnect can resurrect the disconnected provider in memory (only a kind-None hydrate result is safe — it early-returns at state.rs:308 without swapping). Separately, with no single-flight guard, N concurrent startup gate evaluations (chat/assist/cards/coach/provocation/prd/plan-routing) each run their own hydrate and each hits the keyring, multiplying macOS keychain prompts. Fix shape: re-check runtime.is_none() (or compare against the pre-hydrate snapshot) inside the write-lock critical section before installing, and share one in-flight hydrate future across callers.
- **증거**:
  ```
  let next = match tokio::time::timeout(PROVIDER_RUNTIME_HYDRATE_TIMEOUT, hydrate).await { ... providers::validate_model_for_kind(next.kind.as_str(), &next.model)     .map_err(\|e\| e.to_string())?; self.swap_runtime(next.clone())     .map_err(\|e\| format!("runtime: {e}"))?;
  ```
- **검증**: state.rs:313 `swap_runtime(next.clone())` installs the hydrate result with no re-check that the runtime is still None, and no single-flight guard exists anywhere in src-tauri (grep for Semaphore/OnceCell/in-flight flags: zero hits). The window is production-real by the code's own admission: hydrate timeout is 120s (state.rs:42) and PROVIDER_RUNTIME_HYDRATE_TIMEOUT_MESSAGE (state.rs:49-50) explicitly coaches the user through the macOS keychain-unlock prompt, during which only a blocking-pool thread is blocked and Settings remains interactive. Concurrent clobber paths are unserialized async Taur

### [P2] static_preview_base_url TOCTOU: concurrent preview_open IPC calls (student action + frontend reroute invoke) can leak a detached static preview server task+port that survives project switch
- **위치**: `dive/src-tauri/src/ipc/preview.rs`:427  · 유형: 지난 리뷰 누락  · finder: `fresh-rust-ipc`
- **내용**: One imprecision in the finder's scenario: the agent's own preview_open tool (tools/runtime.rs:482-599) never touches the static server — static_file returns asset:// paths and dev_server/auto is refused with preview_panel_required. The realistic concurrent second caller is the frontend's fire-and-forget reroute invoke (useChatSession.ts:477, `void api.invoke("preview_open", {kind: "static_file", source: "reroute"})` on a runtime_routing_decision event) racing a student's PreviewTab action (PreviewTab.tsx:211). The leak interleaving requires both calls to pass the empty check+take (preview.rs:389-409) before either stores at line 427 — i.e. both inside the unguarded bind window — after which the first-stored server is overwritten without abort and its detached task serves the then-current project root on its loopback port until app exit, unreachable by swap_project_root. Fix shape as proposed: impl Drop for StaticPreviewServer calling handle.abort() (also aborting the overwritten instance in the line-427 store), or serialize the whole sequence behind a tokio::Mutex.
- **증거**:
  ```
  if let Some(server) = state.static_preview_server.lock()...take() { server.abort(); } let listener = TcpListener::bind("127.0.0.1:0").await...;  // await with no lock held let mut guard = state.static_preview_server.lock()...; *guard = Some(StaticPreviewServer { root: canonical_root, base_url: base_url.clone(), handle });
  ```
- **검증**: preview.rs:385-433 releases the std::sync::Mutex (state.rs:108) across the TcpListener::bind await: check (389-400), take-abort (402-409), bind (411), and store (423-431) are four separate lock acquisitions. StaticPreviewServer (preview.rs:50-61) has no Drop impl, so the `*guard = Some(...)` overwrite at line 427 drops the loser's JoinHandle, detaching (not aborting) its run_static_preview_server accept loop, which keeps its port bound for the app lifetime; swap_project_root (state.rs:339-343) aborts only the guard-held instance, so a leaked server keeps serving the OLD project root after a pr

### [P2] checkpoint_restore lacks project scoping: a stale-timeline restore around a project switch inserts a wrong-project "auto-pre-restore" row into the audit ledger before failing with a raw git2 error
- **위치**: `dive/src-tauri/src/ipc/checkpoint.rs`:88  · 유형: 지난 리뷰 누락  · finder: `fresh-rust-ipc`
- **내용**: checkpoint_restore (ipc/checkpoint.rs:76-90) resolves the checkpoint by global id against the single app-level dive.db (state.rs:163) with no check that the checkpoint's session belongs to the active project, then runs CheckpointEngine rooted at the current project_root. restore_checkpoint (checkpoint/mod.rs:287) first fully executes create_checkpoint(target.session_id, ..., "auto-pre-restore"): it commits project A's worktree into A's .dive/git and inserts a Checkpoint row attributed to project B's session — unconditionally; the changed_files leak of A's paths is conditional on A's tree having drifted since its last checkpoint. Only then find_commit(target.git_sha) (mod.rs:291) fails with a raw git2 "object not found" that useProductRecovery.ts:133-137 toasts verbatim. No file damage (clear_tracked_worktree at mod.rs:294 is after find_commit). The polluted row then renders in B's own checkpoint timeline as a permanently unrestorable restore point (its sha lives in A's repo). Trigger window is real: Sidebar project switch is in-shell; selectProject awaits project_select (backend root swaps) plus a second session_list IPC before the store re-renders, and runStoreAction sets no loading flag, so the old project's recovery panel stays clickable across that multi-round-trip window. Fix: resolve checkpoint → session → project_id and require it to match the active project before engaging the engine.
- **증거**:
  ```
  // ipc/checkpoint.rs: get_by_id(db.conn(), checkpoint_id) ... no project/session scope check engine.restore_checkpoint(checkpoint_id).map_err(\|e\| e.to_string())?; // checkpoint/mod.rs:287 self.create_checkpoint(target.session_id, target.card_id, "auto-pre-restore", None)?; let oid = git2::Oid::from_str(&target.git_sha)?; let commit = repo.find_commit(oid)?;  // fails: sha lives in the other project's repo
  ```
- **검증**: Every element verified: single global DB (ipc/state.rs:163 dive.db; Session.project_id in db/schema.rs:16) with per-project checkpoint repos (checkpoint/mod.rs:39); no scope check in checkpoint_restore (ipc/checkpoint.rs:82-90); pre-restore create_checkpoint at checkpoint/mod.rs:287 commits the current project's tree and persists a row under the other project's session before find_commit at mod.rs:291 fails; clear_tracked_worktree (mod.rs:294) unreached so no file damage. Reachability confirmed: in-shell Sidebar switch (Sidebar.tsx:131) → selectProject (project-session.ts:471-515) swaps the ba

### [P2] Preview install failure persists the full uncapped package-manager output into the append-only EventLog and chat transcript, bypassing the success path's 80-line cap
- **위치**: `dive/src-tauri/src/ipc/preview.rs`:1225  · 유형: 지난 리뷰 누락  · finder: `fresh-rust-ipc`
- **내용**: On install failure, run_install (preview.rs:1223-1228) returns the full uncapped stdout+stderr of the package-manager run inside the error string, while the success path caps output at MAX_LOG_LINES=80 (push_truncated_lines, lines 26/722/1335). The blob flows via PreviewStartError.message → failed_preview_response (DevServer arm :326, Auto arm :370) → record_preview_events, which writes "message": response.message verbatim into the append-only EventLog (:1047) through append_to_conn (event_log.rs:78 — redaction only, no size cap), on every retry. It is also emitted as the preview_open_result AgentEvent and rendered raw as the chat tool_result summary and execution-evidence summary (chat-session-reducer.ts:236,245). Two corrections to the finder: (1) PreviewTab itself does NOT show the blob — it substitutes the friendly i18n string for dev_server_unavailable (PreviewTab.tsx:191,201, en.json:1679); the raw-display surface is the chat transcript, not a toast. (2) preview_start (whose Err(String) carries the blob directly) is registered in lib.rs:126 but has zero frontend callers. The unbounded command.output() buffering is real but bounded in practice by INSTALL_TIMEOUT=180s. Confirmed unlisted sibling of baseline F3: S-063's run_capped (commit 4fa6699) explicitly replaced cmd.output() only in run_process/terminal_script; preview.rs still calls command.output() with no cap. Fix shape stands: truncate combined to the same line/byte cap before formatting the error (and optionally route the install through process_exec::run_capped).
- **증거**:
  ```
  let combined = format!("{stdout}{stderr}"); if !output.status.success() {     return Err(format!(         "dependency install failed with {}.\n{}",         output.status, combined     )); }
  ```
- **검증**: Chain verified in source: preview.rs:1223-1228 (uncapped error format, exactly as quoted) → :719-721 → failed_preview_response :326/:370 → record_preview_events :1047 writes response.message verbatim; append_to_conn (event_log.rs:78-93) redacts but never truncates. Success-path asymmetry confirmed (MAX_LOG_LINES=80, :722/:1335). Reachable in production: preview_open invoked from PreviewTab.tsx:211 and useChatSession.ts:477, install runs when node_modules is absent (:712). Not baseline: F3 is run_process.rs:95, and S-063 fix commit 4fa6699's message states run_capped replaced cmd.output() "in r

### [P2] project_delete(delete_folder=true) blocklist misses $HOME (and /home, /users, c:/users) — latent recursive home-wipe behind the .dive marker, asymmetric with guard.rs which explicitly blocks agent `rm -rf $HOME`
- **위치**: `dive/src-tauri/src/ipc/project.rs`:91  · 유형: 지난 리뷰 누락  · finder: `fresh-rust-ipc`
- **내용**: Finder was accurate; two sharpenings. (1) The gap is asymmetric with the codebase's own security posture: the agent tool guard names home-wipe as a hazard (guard.rs:65-66 blocks `rm -rf $HOME`; guard.rs:347 blocks $HOME/%USERPROFILE% destructive patterns), yet the IPC delete path — whose dedicated validator and test (project_delete_with_folder_rejects_filesystem_root_and_keeps_db_row) exist precisely to bound unsafe folder deletes — would allow the equivalent on home once ~/.dive exists. (2) The blocklist also misses /home (Linux), /users itself, and c:/users, not just the resolved home dir. Mitigating facts confirmed: app data lives under com.coreelab.dive (auth/mod.rs:294-310), not ~/.dive, so the marker only pre-exists if the user previously opened home as a project; and the only production caller hard-codes deleteFolder=false. Fix shape stands: add resolved home + /users + /home + c:/users to is_system_or_root_path, or require the delete target to be strictly below home.
- **증거**:
  ```
  matches!(trimmed,     "/system" \| "/library" \| "/applications" \| "/bin" \| "/sbin" \| "/usr"         \| "/etc" \| "/private" \| "c:/windows" \| "c:/program files" \| ...) // delete path: if delete_folder { std::fs::remove_dir_all(&root) ... } // only FE caller: Sidebar.tsx:66  await deleteProject(id, false);
  ```
- **검증**: All load-bearing claims verified against code. dive/src-tauri/src/ipc/project.rs:91-104 — is_system_or_root_path blocklist has no entry for $HOME, /users, /home, or c:/users; the parent check (line 49) only applies when file_name() is none, so /Users/<name> passes validate_project_root_for_write. project_open_impl calls ensure_dive_dir on both branches (lines 188, 202), so opening home as a project via the normal picker creates ~/.dive, satisfying the sole extra delete gate (line 70); the delete branch is raw std::fs::remove_dir_all(&root) (line 265) with no FsGuard or capability ACL. Caller c

### [P2] VerifyLog.details test-command summary is the sole locale-unbranched string in verify.rs — Korean sentence for en-locale users, English Debug enum ("Pass"/"Fail") for ko-locale users
- **위치**: `dive/src-tauri/src/dive/verify.rs`:256  · 유형: 지난 리뷰 누락  · finder: `fresh-rust-dive-core`
- **내용**: When a card has a test_command, verify_card appends the executed-command summary to details as a hardcoded Korean format string with a {test_result:?} Debug interpolation (verify.rs:255-259). Every en-locale verify of a test-command card deterministically shows Korean in the student-visible verify log (rendered raw at StepDetailSections.tsx:413-414); every ko-locale verify shows the English Debug variant name inside the Korean sentence. This branch is production-hot because the S-029 evidence gate (approve_eligible → automated_pass_evidence) requires an executed test command for direct Approve. Fix: branch the sentence on prompt_locale_is_english(locale) (locale is already a parameter of verify_card) and map TestResult to a localized word instead of {:?}.
- **증거**:
  ```
  details = if details.is_empty() {     format!("검증 명령 `{command_text}` 실행 결과: {test_result:?}") } else {     format!("{details}\n\n검증 명령 `{command_text}` 실행 결과: {test_result:?}") };
  ```
- **검증**: verify.rs:255-259 hardcodes "검증 명령 `{command_text}` 실행 결과: {test_result:?}" while every other student-facing string in the file branches on prompt_locale_is_english (build_system_prompt:346, build_user_prompt:374, verify_result_schema:410). Reachable in production: card_verify (ipc/cards.rs:846-853) receives the FE locale, both call sites pass the live store value (useWorkmap.ts:298, useChatSession.ts:776), Locale includes "en" (i18n/index.ts:17), and details is rendered raw at StepDetailSections.tsx:413-414 with no translation layer. The test-command branch is actively driven by the evidence 

### [P2] Plan/PRD lifecycle EventLog rows are appended with session_id=NULL, making them unreachable by the sole session-scoped EventLog export despite doc comments and in-tree export contracts stating they exist for export reconstruction
- **위치**: `dive/src-tauri/src/ipc/workspace_plan/plan_steps.rs`:485  · 유형: 지난 리뷰 누락  · finder: `fresh-rust-dive-core`
- **내용**: All plan/PRD lifecycle events (plan_generated, plan_approved, plan_step_appended/changed/retired, plan_step_rationale_challenged, plan_adjustment_offered/accepted/dismissed, prd_authored/edited/version_created, prd_patch_*) are appended with session_id=None, while the only EventLog export path (export/mod.rs:465, ExportEngine::export_session) selects WHERE session_id = ? — NULL rows can never match, so the anonymized pilot JSONL silently omits the entire plan/PRD mutation history. This contradicts three in-tree statements of intent: event_log.rs's module header ("EventLog rows are exported directly to pilot JSONL"), plan_step_retired_payload's doc ("so the export can reconstruct active/retired criterion coverage"), and workspace_plan/artifacts.rs's event_log_export_contracts, which the .dive/plan.json artifact emits to delegate plan_adjustment_offered/accepted/dismissed reconstruction to exactly this export. Sharpest instance: the plan_approved event carries the student's critiqueResponse/critiqueNote, annotated in plan_lifecycle.rs:621-624 as supervision evidence "so the research ledger can distinguish an engaged approval from a blind one (Constitution IV)" — it exists nowhere except the NULL-session EventLog row, so it is entirely unexportable. Partial mitigation: .dive/plan.json snapshots plan_mutations/objections/offers (current state, unanonymized, in the student's project dir), and the in-app dashboard reads plan events via list_plan_events (plan_id GLOB, no session filter). Fix direction: scope the export's event query to also include project-scoped rows for the session's project (Session has project_id), or stamp these events with a session/project discriminator the export understands; rows persist, so the fix is retroactive. Pre-existing before the S-066+ file split (same None args at e533a2b workspace_plan.rs) — a missed finding, not a regression.
- **증거**:
  ```
  plan_steps.rs:485: dive_event_log::append_to_conn(tx, None, dive_event_log::PLAN_STEP_APPENDED_EVENT, payload) export/mod.rs:465: "SELECT id, type, payload, created_at FROM EventLog WHERE session_id = ? ORDER BY id" event_log.rs:1551-1553 doc: "`criterion_ids_retired` records the criteria the retired step had linked so the export can reconstruct active/retired criterion coverage"
  ```
- **검증**: Every cited fact verified read-only: all plan/PRD lifecycle appends pass session_id=None (plan_steps.rs:485,574,703,948,965,1021; plan_lifecycle.rs:432,634; prd_core.rs:216,230,247; prd_interview.rs:138+), and the sole EventLog export (export/mod.rs:465, reached only via export_session at ipc/assist.rs:40 / the "Export Session…" menu) filters WHERE session_id = ?, which by SQL semantics never matches NULL rows. event_log.rs:3 states "EventLog rows are exported directly to pilot JSONL" and plan_step_retired_payload's doc says the field exists "so the export can reconstruct active/retired criter

### [P2] Pre-migration DB backup copies only the main SQLite file, dropping committed-but-uncheckpointed WAL transactions
- **위치**: `dive/src-tauri/src/db/mod.rs`:104  · 유형: 지난 리뷰 누락  · finder: `fresh-rust-infra`
- **내용**: backup_before_forward_migration copies only the main SQLite file while the DB runs in persistent WAL mode with no checkpoint anywhere in the codebase; after a force-quit, the pre-migration backup silently omits the -wal's committed transactions. Note the failure requires manual restore from backups/ (no in-app restore path exists) — the backup is a dev/manual recovery artifact, stale precisely when needed (crash + schema-bump launch). Fix as finder states: rusqlite::backup::Backup or PRAGMA wal_checkpoint(TRUNCATE) before the copy (the pragma needs no extra cargo feature).
- **증거**:
  ```
  let backup_path = backup_dir.join(format!("dive-v{current}-{stamp}.db")); std::fs::copy(path, backup_path)?; // configure_connection (line 110): conn.execute_batch("PRAGMA foreign_keys = ON; PRAGMA journal_mode = WAL;")?;
  ```
- **검증**: db/mod.rs:104 takes the pre-migration safety copy with std::fs::copy of the main .db only, while configure_connection (db/mod.rs:110) puts the production DB (state.rs:163 data_dir/dive.db, opened via AppState::from_app_handle -> migrate() -> backup_before_forward_migration at db/mod.rs:51) permanently in WAL mode; grep confirms zero wal_checkpoint calls anywhere in dive/src-tauri/src, and the only pre-copy operations (connection open, read-only SELECT MAX(version)) do not checkpoint. After a force-quit the -wal sidecar holds committed-but-uncheckpointed transactions, so the v{N} backup written

### [P2] MCP StdioTransport spawns child without kill_on_drop and IPC test-connect/list-tools drop the client without kill() — EOF-ignoring stdio servers linger (dev-build UI only)
- **위치**: `dive/src-tauri/src/ipc/mcp.rs`:226  · 유형: 지난 리뷰 누락  · finder: `fresh-rust-infra`
- **내용**: mcp_server_test_connect (ipc/mcp.rs:226) and mcp_server_list_tools (ipc/mcp.rs:246) drop the McpClient immediately; StdioTransport spawns its tokio::process::Child without kill_on_drop(true) (mcp/transport.rs:131) and its kill() method has only test callers — the sole spawn site in the codebase lacking kill_on_drop (pi_sidecar/transport.rs:116, tools/process_exec.rs:135, ipc/preview.rs:1214/1242 all set it). Correction to the finder's reachability claim: the triggering UI (settings-mcp-dev.tsx '테스트' button) is gated behind import.meta.env.DEV (settings.tsx:88) and absent from release builds, so this is not a first-class production Settings button on student machines; and dropping the transport closes the child's stdin, so spec-compliant MCP servers exit on EOF — only EOF-ignoring/hung npx servers accumulate and survive app exit. Both commands are nonetheless registered in the production invoke_handler (lib.rs:140-141), and the missing kill_on_drop becomes production-relevant as soon as the baseline P1 MCP half-integration is completed. Fix: add .kill_on_drop(true) in StdioTransport::spawn_with_response_timeout (one line), or call kill() before returning from the two IPC commands.
- **증거**:
  ```
  // ipc/mcp.rs:226 (test_connect; :246 list_tools is identical) let (_client, init, tools) = McpServerRegistry::connect_and_initialize(&row)     .await // mcp/transport.rs:128-131 — no .kill_on_drop(true): cmd.stdin(Stdio::piped()).stdout(Stdio::piped()).stderr(Stdio::null()); let mut child = cmd.spawn()?;
  ```
- **검증**: Defect verified line-by-line: mcp/transport.rs:131 spawns the stdio child with no .kill_on_drop(true); ipc/mcp.rs:226 and :246 drop the McpClient immediately after connect_and_initialize; StdioTransport::kill() (transport.rs:148) is called only from two #[cfg(test)] tests (transport.rs:312, 338); no Drop impl exists anywhere in src/mcp/. Decisive contrast: every other child-spawn site in the codebase sets kill_on_drop(true) (pi_sidecar/transport.rs:116, tools/process_exec.rs:135, ipc/preview.rs:1214 and :1242) — the MCP stdio transport is the sole exception to the repo's own hygiene pattern. N

### [P2] Only the active stage's content is mounted — every revisit of the observe stage remounts the coach, firing a fresh LLM guide generation and discarding the typed observation draft
- **위치**: `dive/src/components/product/VerificationReviewStepper.tsx`:172  · 유형: 지난 리뷰 누락  · finder: `fresh-fe-product`
- **내용**: The stepper unmounts inactive stage content, so every revisit of the observe stage remounts VerificationCoachPanel and re-fires the guide-generation effect from a null response — one fresh sidecar LLM call plus coach-requested/evaluated EventLog rows per revisit, with no frontend, API, or backend cache (the panel's own automaticGenerationKey dedup exists but cannot survive unmount). Navigating away also destroys the panel-local typed observation draft; recorded observations survive via lifted manualObservationsByStep, but a draft blocked from recording by the action-backed gate is unavoidably lost. The S-064 E2 fix (keep StepDetailSlideIn mounted) addressed the outer mount boundary only; the inner per-stage boundary has the same class of defect. Fix: keep stage content mounted-but-hidden, or lift the guide response and draft text into StepDetailSlideIn alongside manualObservationsByStep.
- **증거**:
  ```
  {isActive ? (   <div className="ml-7 mt-3" data-testid={`verification-stepper-content-${stage.id}`}>     {stage.content} // VerificationCoachPanel remounts → effect reruns: void generateVerificationCoachGuide({ ...currentRequest, … })
  ```
- **검증**: VerificationReviewStepper.tsx:172 renders stage content only when active (`{isActive ? … : null}`), and VerificationCoachPanel is the stage-2 "observe" content (StepDetailSlideIn.tsx:915). On remount the generate effect (VerificationCoachPanel.tsx:118-155) always re-fires because `response` state dies with the unmount and the `automaticGenerationKey` dedup (line 74) only lives within a mount; the API layer has no cache and the backend `verification_coach_generate_impl` unconditionally logs VERIFICATION_COACH_REQUESTED_EVENT (verification_coach.rs:36) and runs a fresh `run_supervisor_turn` side

### [P2] architectureProposals survives project switch and PRD-authoring open — project A's S-047 architecture cards render and can seed a 'student_confirmed' decision on project B
- **위치**: `dive/src/components/product/useProductShellController.ts`:167  · 유형: 지난 리뷰 누락  · finder: `fresh-fe-product`
- **내용**: The project-switch reset effect (useProductShellController.ts:167-175) omits setArchitectureProposals(null) and handleOpenPrdAuthoring (683) does not clear it either; since ArchitectureProposals carries no project/draft binding and PrdAuthoringBoard gates form cards only on !architecture, stale form-kind proposals from project A's interview turn render as clickable recommend-then-confirm cards on project B's fresh draft, where a click authors a student_confirmed ArchitectureDecision seeded by the wrong project's recommendation. A second path exists via the unguarded .then of submitPrdInterviewTurn (753) when the LLM turn resolves after the switch (prdDraft self-heals through ensurePrdDraft's projectId check; proposals never do). Fix: add setArchitectureProposals(null) to the reset effect and clear it in handleOpenPrdAuthoring (or guard the .then on currentProjectId). Pre-existing before the S-068 hook split (same effect at 8c895a2~1:521-529).
- **증거**:
  ```
  useEffect(() => {   prdDraftRestoreRequestRef.current += 1;   setPrdMode(null); setPrdDraft(null); setCurrentProjectSpec(null);   setPrdPatchFeedback(null); setPrdBusy(false); setPendingPrdPlanRequest(null); }, [currentProjectId]);   // setArchitectureProposals(null) missing // line 753: setArchitectureProposals(result.architectureProposals ?? null);
  ```
- **검증**: useProductShellController.ts:167-175 reset effect clears six PRD states but not architectureProposals; handleOpenPrdAuthoring (683-725) also never clears it (only save:799, quick-intake:963, read-onEdit:1375 do). The type (features/planning/types.ts:85-88) carries no project/draft binding and PrdAuthoringBoard.tsx:366-367 gates formProposals only on kind==='form' && !architecture, so a fresh project-B draft renders project A's cards (903-925) whose click (setArchitectureForm, 474-488) authors a decisionSource:'student_confirmed' ArchitectureDecision seeded by the wrong project's AI recommendat

### [P2] PlanAddStepPanel hardcodes locale "ko-KR" in the scope-expansion supervisor request (plus Korean evidence-ref labels), forcing Korean supervision cards for en-locale users — distinct from the baseline PlanDraftApprovalScreen instance
- **위치**: `dive/src/components/product/PlanAddStepPanel.tsx`:385  · 유형: 지난 리뷰 누락  · finder: `fresh-fe-product`
- **내용**: Finder was accurate. Sharpened consequence: the backend branch at dive/src-tauri/src/ipc/provocation_agent.rs:527 (`context.locale.starts_with("en")`) makes the hardcode concretely consequential — the deterministic scope-drift question and supervision_habit strings render in Korean for English-locale users, and the Rust-side `request.locale.unwrap_or("ko-KR")` (lines 408-469) shows omitting locale (or passing the store locale, as all four StepDetailSlideIn sites do at :511/:529/:555/:575) is the intended pattern. Fix is one line plus translating the six evidence-ref label literals (lines 208/218/227/236/246/258); the sibling hardcode in PlanDraftApprovalScreen.tsx:260 is already baseline-tracked and could be fixed in the same pass.
- **증거**:
  ```
  return createScopeExpansionSupervisorRequest({   sessionId: supervisorSessionId,   projectId,   planId,   artifactRef,   sourceUiMode: provocationMode,   locale: "ko-KR",
  ```
- **검증**: PlanAddStepPanel.tsx:385 hardcodes locale:"ko-KR" in createScopeExpansionSupervisorRequest; adapters.ts:1406 only falls back to useLocaleStore when request.locale is nullish, so the hardcode always wins, and the backend genuinely branches output language on it (provocation_agent.rs:527 `starts_with("en")` selects English vs Korean question/supervision_habit). Reachable in production: the request feeds evaluateProvocationSupervisor at PlanAddStepPanel.tsx:527 in an ungated effect (DEV check at :551 gates only logging). Korean evidence-ref labels at lines 208/218/227/236/246/258 confirmed. Not b

### [P2] refreshCheckpoints has no stale-response guard (unlike useWorkmap's generation counter), and checkpoint_restore does no session-ownership check — a late checkpoint_list from the previous session can display and silently restore another session's checkpoint
- **위치**: `dive/src/components/product/useProductRecovery.ts`:68  · 유형: 지난 리뷰 누락  · finder: `fresh-fe-product`
- **내용**: refreshCheckpoints (useProductRecovery.ts:68-84) sets checkpoints unconditionally from a listCheckpoints closure that captures sessionId (useChatSession.ts:796-801). On session switch (Sidebar session click, PlanDashboardPanel step jump) the effect refires for the new session, but an in-flight fetch from the old session — plausible because streaming-end (controller:1168) and manual refresh (controller:1615) also trigger fetches — can resolve last and overwrite the list; there is no generation guard, cleanup, or remount (MainShell mounts the shell unkeyed). The sharpened part: the backend compounds it — checkpoint_restore (ipc/checkpoint.rs:76-105) accepts a bare checkpoint_id with no validation that it belongs to the current session, so clicking restore on a stale row from a same-project sibling session silently restores that session's workspace snapshot while useChatSession reloads the current session's message history, leaving chat and workspace diverged. Cross-project stale restores fail loudly (SHA missing from the new project's checkpoint repo). Fix is mechanical: replicate useWorkmap.ts:148-186's requestGeneration pattern, and/or have checkpoint_restore verify target.session_id against an expected session id passed from the frontend.
- **증거**:
  ```
  const refreshCheckpoints = useCallback(async () => {   …   const rows = await listCheckpoints();   setCheckpoints(rows);          // no generation / session guard   setCheckpointsError(null); }, [currentSessionId, listCheckpoints]);
  ```
- **검증**: useProductRecovery.ts:68-84 applies setCheckpoints(rows) with no generation/session guard; useChatSession.ts:796-801 listCheckpoints closes over sessionId so an in-flight checkpoint_list carries the old session; MainShell.tsx:5-6 mounts the controller unkeyed so hook state survives session switches; switching is a real flow (Sidebar.tsx:256, PlanDashboardPanel.tsx:109/139) and refreshCheckpoints also fires on streaming end (useProductShellController.ts:1168) and manual refresh (:1615), leaving fetches in flight at switch time. The restore consequence survives too: src-tauri/src/ipc/checkpoint.

### [P2] agent_progress/tool_call_start wipe a pending cancelRequested — in-flight-event race briefly re-enables the cancel button
- **위치**: `dive/src/hooks/chat-session-reducer.ts`:455  · 유형: 지난 리뷰 누락  · finder: `fresh-fe-features-stores`
- **내용**: The reducer defect is real, but the finder's mechanism overstates the window: cancellation is not merely "checked at loop boundaries." The supervised Pi read loop races wait_for_cancel (10ms poll, pi_sidecar/transport.rs:204-208) against every event read (pi_sidecar.rs:677-689) and inside tool execution (731-758), so backend event emission stops within ~10ms of chat_cancel; the sidecar is then killed and chat_send rejects, whose catch path (appendErrorMessage, useChatSession.ts:330-346) clears isStreaming and cancelRequested. agent_progress is also throttled to one per 5s (PROGRESS_EVENT_MIN_INTERVAL, pi_sidecar.rs:72). The reachable failure is therefore an in-flight event emitted just before the backend honored the token and delivered to the webview after cancel() set the flag: it wipes the acknowledgment, re-enabling the button and reverting the label for the sub-second remainder of teardown. Rare, cosmetic, and functionally harmless (re-clicking chat_cancel is idempotent), but semantically wrong — progress does not mean the cancel was processed; only terminal events (assistant_end/error/done) or a new run start should clear cancelRequested. P2.
- **증거**:
  ```
  case "agent_progress": {   if (runTerminal) return prev;   return { ...prev, isStreaming: true,     runStartedAt: prev.runStartedAt ?? evt.created_at,     cancelRequested: false };   // wipes pending-cancel state on every heartbeat }
  ```
- **검증**: Code facts verified exactly: dive/src/hooks/chat-session-reducer.ts:449-456 (agent_progress, runTerminal=false) and :310-331 (tool_call_start) both return cancelRequested: false; ChatArea.tsx:383-387 drives the cancel button's disabled state and label from that flag; useChatSession.ts:530-539 feeds every delivered event to the reducer with no pending-cancel guard; cancel() (useChatSession.ts:661-667) sets the flag and chat_cancel (src-tauri/src/ipc/chat.rs:1035-1043) only stores an AtomicBool. No test or comment encodes the reset as intended, and the cancel token always exists during a run (Ac

### [P2] normalizeFailureKey replaces digits before the 0x-hex pattern, making the hex normalization dead code and fragmenting retry-loop fingerprints for letter-bearing hex tokens; adjacent literal no-op replace
- **위치**: `dive/src/features/provocation/adapters.ts`:212  · 유형: 지난 리뷰 누락  · finder: `fresh-fe-features-stores`
- **내용**: In normalizeFailureKey (adapters.ts:209-215), .replace(/\d+/g,"#") runs first and replaces the "0" in every "0x" prefix, so .replace(/0x[0-9a-f]+/g,"0x#") can never match any input — it is entirely dead, not merely inconsistent. Finder's examples were wrong ('0xdead' actually → '#xdead', not '0x#'; '0x1f4a' → '#x#f#a'), but the consequence is confirmed and stronger: 0x-hex tokens containing [a-f] are never collapsed, so two occurrences of the same crash differing only in a memory address/hash get different errorKeys. This fragments retrySignalsFromConversation buckets (used by MessageList.tsx:81) and resets StepDetailSlideIn's per-step failureCount (StepDetailSlideIn.tsx:245-250), so buildRetryLoopReviewAssessment's failureCount >= 2 same_failure_repeated/eligible gate (adapters.ts:973/988) is missed exactly in noisy-failure cases. .replace(/line #/g,"line #") is a literal no-op (harmless — digit replace already normalizes line numbers). Fix: put the hex replace before the digit replace (or use /0x[0-9a-f#]+/ after), and delete the no-op line. Pre-existing since f9f1f6a; not touched by cleanup commits; not in either baseline doc.
- **증거**:
  ```
  function normalizeFailureKey(value: string): string {   return lowercase(value)     .replace(/\d+/g, "#")     .replace(/0x[0-9a-f]+/g, "0x#")   // digits already gone → mixed hex mangled     .replace(/line #/g, "line #")      // literal no-op     .slice(0, 240);
  ```
- **검증**: Reproduced read-only with node: dive/src/features/provocation/adapters.ts:209-215 runs .replace(/\d+/g,"#") first, which replaces the "0" of every "0x" prefix, so the subsequent /0x[0-9a-f]+/g can never match — hex normalization is fully dead code (stronger than the finder's "inconsistent" claim). '0xdeadbeef' vs '0xcafebabe' yield distinct keys ('...#xdeadbeef...' vs '...#xcafebabe...'). Reachable in production: retrySignalsFromConversation (adapters.ts:332/338) is called from MessageList.tsx:81, and normalizeFailureFingerprint from StepDetailSlideIn.tsx:236, where failureCount increments onl

### [P2] adaptAcceptanceCriteria valid-id branch skips the dedupe its own invalid-id branch performs, letting rare full-shape LLM duplicates render pre-approval (no persisted-data or coverage-truth impact)
- **위치**: `dive/src/features/planning/projectSpec.ts`:146  · 유형: 지난 리뷰 누락  · finder: `fresh-fe-features-stores`
- **내용**: Real code defect, reproduced: adaptAcceptanceCriteria's first branch (valid shape + valid id, projectSpec.ts:146-149) pushes without the uniqueness check the second branch has (line 157), so full-shape items repeating an id pass through as duplicates — diverging from the canonical Rust normalize_acceptance_criteria (prd_core.rs:429-434), which reallocates via used_ids. projectSpec.test.ts covers only invalid AC-000 reassignment, not duplicate valid ids. However the finder's impact is overstated: (1) all persisted paths are dupe-safe before the FE sees them (PRD snapshots normalize on save; PRD-interview patches allocate ids backend-side in prd_patch.rs:90-103; step rows are backend-built), leaving only raw LLM plan-draft output (decodeWorkspacePlanDraftFromLlm) — where the prompt declares criteria as sentence strings and shows the LLM only {criterionId,text}; strings get fresh unique ids, and echoed {criterionId,text} objects are dropped by the shape check (missing source/status/createdInVersion), so triggering requires the LLM to hallucinate the full camelCase wire shape with repeated ids — the documented real malformed mode (bare "AC-001" text echoes, prd_core.rs:452-462) cannot trigger it; (2) the "silently mislinked verification coverage" round-trip claim fails: plan approval hard-fails any step linked id not among active saved-PRD criterion ids (plan_lifecycle.rs:60-67) and persisted step criteria resolve linked ids PRD-first (plan_lifecycle.rs:84-93), so FE duplicate ids never become coverage truth, and FE reallocation would not fix that scenario. Residual real impact: duplicate React keys on the pre-approval PlanDraftApprovalScreen criterion list (key={criterion.criterionId}, line 536) and linkedCriteriaFor Map collapse in the rendered draft — display-level, pre-approval only. Fix remains the one-line dedupe/reallocate in the first branch plus a duplicate-valid-id test.
- **증거**:
  ```
  for (const item of value) {   if (isAcceptanceCriterion(item)) {     criteria.push({ ...item, text: item.text.trim(), criterionId: item.criterionId.trim() });     continue;   // no duplicate-id check — only the invalid-id branch below has one   }
  ```
- **검증**: Defect verified by reading dive/src/features/planning/projectSpec.ts:145-161 (first branch has no criteria.some check; second branch does) and by mechanical reproduction of the transcribed logic (node: [full-shape AC-001, full-shape AC-001] -> ['AC-001','AC-001']); Rust counterpart dedupes (dive/src-tauri/src/ipc/workspace_plan/prd_core.rs:429-434); test gap confirmed (projectSpec.test.ts:65 only tests invalid AC-000). Not baseline: docs/qa/public-release-code-review-2026-07-20.md's only planning-FE item is C7 (prdPatch.ts, different deleted file) and no D-013 entry in specs/013-public-release

### [P2] usePlan and usePlanRoadmap refresh has no generation guard — a late response from the previous project overwrites the current project's state
- **위치**: `dive/src/features/planning/usePlan.ts`:170  · 유형: 지난 리뷰 누락  · finder: `fresh-fe-features-stores`
- **내용**: usePlan.refresh (usePlan.ts:170-176) and usePlanRoadmap.refresh (usePlanRoadmap.ts:163-198) unconditionally setStatus/setPrdStatus/setSteps after awaiting IPC, with no generation/staleness guard. The hosting hook instance survives project switches (MainShell is unkeyed in App.tsx; useProductShellController.ts:159-160 passes a store-driven currentProjectId that swaps in place via Sidebar selectProject), and the switch-reset effect (useProductShellController.ts:167-175) clears PRD local state but not plan.status/prdStatus. So on a rapid A→B switch, a late-resolving refresh_A stomps B's state — and it persists, because usePlan.refresh only re-runs on the next mutation. Stale state feeds real gates: usePlanDraftLifecycle.ts:149 routes plan generation through the replace-approved-plan confirmation on stale has_approved_plan (or, inverted, lets it hard-fail on the backend's "already has approved plan"), and usePlanChatRouting.ts:109-114 derives approvedPlanId from stale status.plan_id and passes it as planId into requestPlanAddStepDraft — a cross-project plan id. The sibling useWorkmap (useWorkmap.ts:128,149,167,177,181,184) guards exactly this race with a generation ref, so the hazard class is acknowledged in the codebase; these two hooks just lack the guard.
- **증거**:
  ```
  const [next, nextPrd] = await Promise.all([   api.invoke<WorkspacePlanStatus>("workspace_plan_status", { projectId }),   api.invoke<WorkspacePrdStatusWire>("workspace_prd_status", { projectId }), ]); setStatus(next);                    // no generation/staleness check setPrdStatus(normalizePrdStatus(nextPrd ?? next.prd_status));
  ```
- **검증**: Evidence quote matches usePlan.ts:170-176 verbatim; identical unguarded pattern at usePlanRoadmap.ts:163-198; generation-ref guard present in useWorkmap.ts:128-184 as claimed. Reachable: MainShell unkeyed (App.tsx:199), hooks persist across Sidebar selectProject switches (Sidebar.tsx:131), reset effect (useProductShellController.ts:167-175) does not clear plan status, and no periodic/corrective refresh exists — a stale last write persists until the next mutation-triggered refresh. Downstream gating confirmed at usePlanDraftLifecycle.ts:149 (has_approved_plan → replace confirmation) and usePlan

### [P2] 'Reset approval shortcuts next session' checkbox is a dead control: no consumer of dive:reset-next-session, and the desktop (Tauri) load path never reads it back so unchecking silently reverts
- **위치**: `dive/src/pages/settings.tsx`:681  · 유형: 지난 리뷰 누락  · finder: `fresh-fe-shell-pages`
- **내용**: The Safety-section checkbox (settings.tsx:678-697) writes `dive:reset-next-session` (line 684) that nothing consumes — Rust cannot read webview localStorage, and the only FE read (line 181) is in the non-Tauri demo branch, unreachable in the desktop app because the Tauri branch returns at line 176. Consequently in production the control (a) has no effect — the auto-approve policy is an in-memory Arc<RwLock<AutoApprovePolicy>> reset to default on every launch (ipc/state.rs:125/183; provider_policy_get/set in ipc/policy.rs touch only memory, and dive:auto-approve-policy localStorage is never rehydrated into the backend) — and (b) does not even persist visually: resetNextSession re-initializes to useState(true) on every settings mount, so a user who unchecks it finds it re-checked on the next visit. Actual behavior coincidentally matches the checked default (approvals do reset each session), so there is no security regression, but the advertised unchecked state ("keep my approval shortcuts across sessions") is silently impossible, and a safety-labeled setting misrepresents both its persistence and its effect in a supervision-focused beginner tool. Fix options: delete the control, or make it real (persist the flag and honor it at policy init).
- **증거**:
  ```
  settings.tsx:124  const [resetNextSession, setResetNextSession] = useState(true); settings.tsx:176-177  (Tauri branch) return;  // never reads dive:reset-next-session settings.tsx:684  window.localStorage.setItem("dive:reset-next-session", ...) $ grep -rn "reset-next-session" src src-tauri/src \| grep -v settings.tsx (no results — zero consumers)
  ```
- **검증**: Every factual claim verified read-only. (1) Zero consumers: repo-wide grep shows `dive:reset-next-session` only at dive/src/pages/settings.tsx:181 (read, non-Tauri demo branch) and :684 (write); nothing in src-tauri/, scripts, or tests reads it, and testid `policy-reset-next` has no test consumers. (2) Desktop app never reads it back: the load effect's Tauri branch returns at settings.tsx:176, before the :181 read, so `resetNextSession` re-inits to `useState(true)` (line 124) on every settings mount — unchecking silently reverts. (3) No hidden guard: provider_policy_get/set (src-tauri/src/ipc/

### [P2] Shared apiKeyInput state carries a typed key across provider connect forms — stale masked key is submittable to the wrong provider
- **위치**: `dive/src/pages/settings.tsx`:126  · 유형: 지난 리뷰 누락  · finder: `fresh-fe-shell-pages`
- **내용**: All provider cards share one apiKeyInput (settings.tsx:126), cleared only on successful connect (255). The card expand/collapse handler (569-572) resets connectError but not apiKeyInput, so expanding Anthropic, pasting a key, then clicking another GA provider's connect button (anthropic/openai/openrouter all share the form) re-renders the same key — masked as password dots — into that provider's input with the submit button enabled, letting the user submit provider A's key to provider B and hit a confusing auth failure (surfaced since S-062, but still a wrong-party key transmission). One-line fix: clear apiKeyInput whenever expandedKind changes (e.g., setApiKeyInput("") alongside setExpandedKind in the 569-572 handler).
- **증거**:
  ```
  settings.tsx:126  const [apiKeyInput, setApiKeyInput] = useState(""); settings.tsx:569  onClick={() => { 570    setConnectError(null); 571    if (isCodex) setCodexDialogOpen(true); 572    else setExpandedKind(expanded ? null : p.kind);  // apiKeyInput not cleared
  ```
- **검증**: Evidence verified exactly as quoted: settings.tsx:126 declares the single shared apiKeyInput; the only clearing call in the file is settings.tsx:255, reached only after a successful connect (grep shows 7 references, 1 clear). The connect-button handler at 569-572 resets connectError but not apiKeyInput when switching expandedKind. Every non-codex GA card (anthropic/openai/openrouter, settings.tsx:46-58 all ga:true) renders the same controlled value={apiKeyInput} into a type="password" input (614-625), and the submit button (629) is enabled by the stale value — so typing an Anthropic key then c

### [P2] Codex OAuth refresh token persists in temp dive-pi-sidecar-* dirs after any exit that skips Drop (crash, SIGKILL, or app quit mid-turn); no startup sweep
- **위치**: `dive/src-tauri/src/pi_sidecar/credential.rs`:18  · 유형: 지난 리뷰 누락  · finder: `fresh-sidecar-node`
- **내용**: Sharper than the finder's crash-only framing: Tauri shutdown exits via std::process::exit, which skips destructors of live locals on other runtime threads, so an ordinary app quit while a supervised turn is in flight can also strand the dir — a far more common classroom event than SIGKILL/power loss. Each stranded dir holds a plaintext auth.json with a long-lived refresh token; nothing ever reclaims them (Windows does not purge %TEMP%). Fix is the finder's suggestion: best-effort remove of temp_dir()/dive-pi-sidecar-* at app startup (safe — dir names are per-turn UUIDs, and any concurrently live dir belongs to the same just-started process, so a startup-time sweep races nothing).
- **증거**:
  ```
  credential.rs:18  let path = std::env::temp_dir().join(format!("dive-pi-sidecar-{}", uuid::Uuid::new_v4())); credential.rs:39-41  impl Drop for TempAuthDir { fn drop(&mut self) { let _ = std::fs::remove_dir_all(&self.path); } } grep -rln remove_dir_all src/ -> only credential.rs (Drop) and ipc/project.rs (unrelated); no startup sweep
  ```
- **검증**: credential.rs:18 writes auth.json (access+refresh token+accountId, credential.rs:87-100) to temp_dir()/dive-pi-sidecar-<uuid> on every supervised Codex turn (production callers pi_sidecar.rs:349 run_supervisor_turn and :603 run_supervised_turn_inner; parity.rs:27 fixes Codex to CredentialMode::OauthFile). Sole cleanup is TempAuthDir::Drop (credential.rs:38-42); grep confirms remove_dir_all exists nowhere else relevant, no dive-pi-sidecar-* sweep in lib.rs/main.rs/pi_sidecar, and the Node sidecar (pi-sidecar/src/index.mjs:167) reads auth_path but never deletes it. Baseline docs clear: the revie

### [P2] JSONL protocol has no defense against stray stdout writes from the bundled Pi SDK — one stray line hard-fails the student's turn
- **위치**: `dive/pi-sidecar/src/index.mjs`:40  · 유형: 지난 리뷰 누락  · finder: `fresh-sidecar-node`
- **내용**: The sidecar's stdout is the JSONL protocol channel (index.mjs emit → process.stdout.write) but nothing pins console.log/stdout away from the ~60-package bundled SDK graph (openai, @aws-sdk, google-auth-library, gcp-metadata, debug, ws...), and the SEA build banner stubs fs only. Any stray stdout line — including an empty console.log() — fails serde on the strictly-tagged SidecarEvent enum and is terminal in both turn paths (run_supervised_turn_inner via `event?`, run_supervisor_turn via direct `from_str?` + child kill). On the list_models handshake it caches RegistryState::Unavailable sticky for the process lifetime; note this last consequence is a designed fail-open degradation (preflight disabled, executable() returns None), softer than a broken registry. The asymmetry is telling: query_model_registry deliberately tolerates unexpected protocol events, and the sidecar's own inbound parser skips blank/invalid lines and continues, yet the Rust turn readers hard-fail on the first unparseable line. Low likelihood with the pinned 0.80.6 SDK (live QA clean, debug logs to stderr), but a single-point protocol fragility an SDK patch bump could trigger; conventional one-line fix (console.log = console.error, or reserve stdout via dedicated fd).
- **증거**:
  ```
  index.mjs:39-41  export function emit(message) { process.stdout.write(`${JSON.stringify(message)}\n`); } grep -n "console\." dive/pi-sidecar/src/*.mjs -> no matches (no redirect anywhere) transport.rs:64-69  Err(err) => SidecarReadMessage::Error(format!("parse sidecar event: {err}: {}", redact_line(&line)))
  ```
- **검증**: Chain fully verified read-only: index.mjs:39-41 emits protocol JSONL on process.stdout with no console/stdout redirect anywhere in sidecar runtime source (grep: only console usage in the package is build-time build-sidecar.mjs); the esbuild SEA bundle (build-sidecar.mjs:244-276) inlines the ~60-package pinned SDK graph (openai, @aws-sdk, google-auth-library, gcp-metadata, debug, ws confirmed in node_modules) with a banner that stubs fs only — no stdout protection — and new_sidecar_process (pi_sidecar.rs:78) adds none. Any non-JSON stdout line fails serde on the strictly-tagged SidecarEvent enu

### [P2] verify-version-sync cannot detect a stale README version badge (§3 never asserts `expected` against README) and is wired into no workflow
- **위치**: `dive/scripts/verify-version-sync.mjs`:91  · 유형: 지난 리뷰 누락  · finder: `verify-v4-audit`
- **내용**: Post-D-013-04, verify-version-sync §3 checks README only for frozen rc.1/rc.2 historical markers, the removed Phase-6 row, and the ADR count; `expected` (defaulted from package.json) is asserted against CHANGELOG but never against README's version badge (README.md:10) or roadmap rows. A normal release bump (package.json/Cargo.toml/Cargo.lock/tauri.conf.json + new CHANGELOG header) with a stale badge passes clean — the recurrence of baseline A3 with the gate green. Compounding: no workflow invokes verify:version-sync (verify:v4 excludes it; release.yml's verify-versions job re-implements only the three §1 file checks against the dispatch tag, skipping Cargo.lock and README), so even its existing checks depend on manual invocation. Fix: add a badge assertion (e.g. `readme.includes(expected.replace(/-/g, "--"))` or a shields-badge regex) to §3 and chain version-sync into verify-v4-all or the release-gate Gate A command list.
- **증거**:
  ```
  sim: pkg/Cargo.toml/Cargo.lock/tauri.conf bumped to 1.0.0-rc.10, CHANGELOG '## [1.0.0-rc.10]' added, README badge left 'version-1.0.0--rc.9' → verify-version-sync EXIT=0 (PASS 45 / FAIL 0) §3 checks only rc.1-yank/rc.2-wiring markers + ADR count; no `expected` assertion against README $ grep -rn version-sync .github/workflows/ → no workflow invokes it (release.yml re-implements §1 minus Cargo.lock)
  ```
- **검증**: Every factual claim survived attack. (1) Missing check: full read of dive/scripts/verify-version-sync.mjs shows §3 (lines 90-107) asserts README only against frozen historical markers — Phase-6 row removed, rc.1 yank, rc.2 production-wiring, ADR count — while `expected` is asserted only against CHANGELOG (line 108: `changelog.includes(`## [${expected}]`)`). The README badge (README.md:10, `version-1.0.0--rc.9`) and roadmap rows are never compared to `expected`, so the finder's rc.10 simulation outcome is deterministic from the code path (current live run: PASS 45 / FAIL 0 / WARN 2), i.e. the e

### [P2] ESLint flat config applies zero rules to any .mjs file — pi-sidecar production runtime and all 21 CI-gating verifier scripts have no static analysis despite the lint gate in build.yml/release-gate.yml
- **위치**: `dive/eslint.config.js`:14  · 유형: 지난 리뷰 누락  · finder: `ci-deps-build`
- **내용**: Verified via --print-config: .mjs files are linted (not ignored — the ignores list excludes pi-sidecar/build but not pi-sidecar/src) but with an empty rules set, so `eslint . --max-warnings 0` can only ever surface parse errors on them. Affected: pi-sidecar/src/index.mjs (339 lines, production sidecar runtime incl. redactError), pi-sidecar/src/main.mjs, pi-sidecar/build-sidecar.mjs (build tooling), and the 21 scripts/*.mjs verifiers that the CI gates themselves execute. They are equally outside tsc (tsconfig include: ["src"]). The finder's side-question is also correct: S-068 extracted hooks are .ts/.tsx under src/ and fully covered. Fix is a one-object addition: { files: ["**/*.mjs"], extends: [js.configs.recommended], languageOptions: { globals: globals.node } }.
- **증거**:
  ```
  eslint.config.js:14  files: ["**/*.{ts,tsx}"],   (the only extends of js/tseslint recommended) eslint.config.js:33  files: ["*.config.{js,ts}", "vite.config.ts"],  (languageOptions only, no rules) $ wc -l pi-sidecar/src/*.mjs -> 339 src/index.mjs, 7 src/main.mjs tsconfig.json:23  "include": ["src"]
  ```
- **검증**: Reproduced read-only: `npx eslint --print-config pi-sidecar/src/index.mjs` returns "rules": {} — ESLint visits .mjs files under `eslint .` but applies zero rules, since the only rule-bearing config object is scoped files: ["**/*.{ts,tsx}"] (dive/eslint.config.js:14) and the *.config.{js,ts} object (line 33) sets only node globals. Same result for scripts/*.mjs (21 CI-gating verifiers, package.json:31-38). These files are also outside tsc (dive/tsconfig.json:23 include: ["src"]) and pi-sidecar/package.json has no lint of its own, so the 339-line production sidecar runtime (pi-sidecar/src/index.

### [P2] Public release SOP (docs/packaging-windows.md) still cites nonexistent internal .wily/phases files missed by the D-013-15 neutralization sweep
- **위치**: `docs/packaging-windows.md`:94  · 유형: 지난 리뷰 누락  · finder: `secrets-tree-hygiene`
- **내용**: Lines 94 and 185 of docs/packaging-windows.md instruct use of four `.wily/phases/p10-09-external-release-blockers/*` files that are neither tracked nor present locally, while lines 96/106/138 of the same doc were already neutralized to "internal ... (공개 저장소 미포함)" phrasing — the .wily references are a missed spot in that sweep. Additionally (same defect class, same fix pass): line 183 cites `internal/DIVE_NEXT.md`, which is also untracked and nonexistent. Fix by applying the same neutral "internal notes, not in the public repository" phrasing or inlining the required wording.
- **증거**:
  ```
  docs/packaging-windows.md:94: 승인 요청은 `.wily/phases/p10-09-external-release-blockers/approval-packet.md`의 정확한 문장을 사용하고, 커밋 범위는 `.wily/phases/.../release-prep-commit-manifest.md`의 명시적 staging 목록을 따른다. $ git ls-files .wily → (empty); $ ls .wily → No such file or directory
  ```
- **검증**: docs/packaging-windows.md:94 and :185 direct the release owner to `.wily/phases/p10-09-external-release-blockers/{approval-packet,release-prep-commit-manifest,external-evidence-request,handoff}.md`, but `git ls-files .wily` is empty and `.wily/` does not exist even locally; the doc is live-public (linked from README.md:72 and :88) and is the ONLY tracked file referencing `.wily/phases`. Not baseline: the 2026-07-20 review's sole wily item is E6 (verify-version-sync wilyChecks script hardcoding — a different defect) and no D-013 decision covers it. Decisive evidence it is an oversight, not a de

### [P2] Tracked create-tauri-app template assets missed by dead-code sweep: orphaned dive/public/tauri.svg + stock vite.svg still wired as the app favicon (dive/index.html:5)
- **위치**: `dive/public/tauri.svg`:1  · 유형: 지난 리뷰 누락  · finder: `secrets-tree-hygiene`
- **내용**: dive/public/tauri.svg (tracked, zero references anywhere) and dive/public/vite.svg (referenced only as the favicon at dive/index.html:5) are the unmodified create-tauri-app scaffold assets; both ship in every release bundle via Vite's default publicDir copy. Same species as baseline C2 (`greet`, fixed in S-060) but public/ was never swept. Fix is trivial: delete tauri.svg and replace or drop the vite.svg favicon link — proper DIVE icons already exist under dive/src-tauri/icons/.
- **증거**:
  ```
  $ git grep -l "tauri.svg" → (no results) dive/index.html:5: <link rel="icon" type="image/svg+xml" href="/vite.svg" />
  ```
- **검증**: Verified read-only: dive/public/tauri.svg and vite.svg are git-tracked verbatim create-tauri-app stock assets; `git grep tauri.svg` returns zero hits repo-wide (pure orphan), and vite.svg's only reference is dive/index.html:5 as the webview favicon — so the release bundle (default Vite publicDir copies public/ into dist/) ships the stock Vite logo as favicon and a dead Tauri logo. Not baseline: review item C2 names only the `greet` command as template residue (since fixed in lib.rs), and no D-013 decision or specs/013 file mentions the SVGs/favicon (grep empty). Severity stays P2: Tauri deskto

### [P2] Five stale tracked .gitkeep placeholders; dive/src/components/sidebar/ is an empty bootstrap-era directory with zero importers and zero tooling references
- **위치**: `dive/src/components/sidebar/.gitkeep`:1  · 유형: 지난 리뷰 누락  · finder: `secrets-tree-hygiene`
- **내용**: dive/src/components/sidebar/ contains only a 0-byte .gitkeep from the initial scaffold commit (53e6f80); nothing imports from it (the real sidebar lives at components/shell/Sidebar.tsx) and no script/CI/doc references the path, so the directory plus its .gitkeep can be deleted outright. The four other .gitkeeps (components/workmap, hooks, pages, stores) sit in directories that now hold 8–21 real tracked files, so they no longer serve their keep-empty-dir purpose and can be dropped. Zero runtime impact; fix is `git rm` of 5 files (which removes the empty sidebar dir). P2 tree hygiene appropriate for the public-flip round.
- **증거**:
  ```
  $ git ls-files \| grep gitkeep → 5 files (sidebar, workmap, hooks, pages, stores) $ git ls-files dive/src/components/sidebar → only .gitkeep (0 other files) $ grep -rn "components/sidebar" dive/src --include="*.ts*" → (no results)
  ```
- **검증**: All evidence reproduced read-only: `git ls-files \| grep gitkeep` yields exactly 5 files (dive/src/components/sidebar/.gitkeep, components/workmap/.gitkeep, hooks/.gitkeep, pages/.gitkeep, stores/.gitkeep); `git ls-files dive/src/components/sidebar` shows the directory holds ONLY the 0-byte .gitkeep (added in bootstrap commit 53e6f80, May 3); workmap/hooks/pages/stores hold 9/8/21/9 real tracked files respectively, making their placeholders redundant. grep for "components/sidebar" across dive/src, dive/src-tauri/src, dive/scripts, docs, and .github returns zero hits — the live sidebar UI is co

### [P2] persist_prd_delta_for_plan_mutation records stale criterion ids in the mutation/event ledger when a proposed id collides and is reallocated in the snapshot
- **위치**: `dive/src-tauri/src/ipc/workspace_plan/plan_steps.rs`:112  · 유형: 지난 리뷰 누락  · finder: `gap1-ipc/workspace_plan/*`
- **내용**: Confirmed as filed, with two sharpenings. (1) The divergence trigger is broader than id *reuse*: normalize_plan_mutation_delta -> normalize_acceptance_criteria (prd_core.rs:415-450) allocates ids from the delta-local list only, so an added criterion sent with NO id (the natural shape for a proposed new criterion) is assigned "AC-001" and then deterministically collides with any non-empty snapshot — the apply loop (plan_steps.rs:106-118) reallocates the id only on the clone pushed into next.acceptance_criteria while delta.added_criteria keeps the stale id, which is what gets persisted in the ProjectSpecVersion delta_summary (line 141), PRD_EDITED added_ids (155-159), PRD_VERSION_CREATED payload, NewPlanMutation.prd_delta (462), the plan_step_appended payload (474), and exported plan.json planMutations. (2) Reachability caveat: no currently-wired UI flow triggers it — PlanAddStepPanel.buildPrdDelta always sends addedCriteria: [] (PlanAddStepPanel.tsx:167), usePlanRouter sends prdDelta: null, and the batch path passes None per draft — so the defect is latent, not live. But workspace_plan_append_step/supersede_step (commands.rs:217-268) accept prd_delta.added_criteria as a supported S-033 contract (the sole test, workspace_plan_ipc.rs:2996, exercises it end-to-end with a conveniently non-colliding AC-002), and no downstream guard reconciles the ledger against the snapshot. Latency caps this at P2.
- **증거**:
  ```
  criterion.criterion_id = allocate_acceptance_criterion_id(&next.acceptance_criteria); ... next.acceptance_criteria.push(criterion); } ... let delta_summary = delta_summary_for_plan_mutation(&delta); // stale ids let added_ids = delta.added_criteria.iter().map(\|c\| c.criterion_id.clone())
  ```
- **검증**: Divergence verified in code: plan_steps.rs:102-119 reallocates colliding ids only on the clone pushed to the snapshot while delta.added_criteria retains the stale id, and that stale delta is persisted at plan_steps.rs:141 (version delta_summary), 155-173 (PRD_EDITED added_ids), 176-188 (PRD_VERSION_CREATED), 462 (NewPlanMutation.prd_delta), 474 (plan_step_appended). prd_core.rs:420-434 proves normalize dedups only delta-locally (used_ids/out start empty) and manufactures colliding "AC-001" ids for id-less criteria via allocate_acceptance_criterion_id(&out). Refutation attempts failed: no snaps

### [P2] Seven plan-mutation write sites export artifacts and create pre-pivot checkpoints into the ambient selected-project root without validating it against the plan's project
- **위치**: `dive/src-tauri/src/ipc/workspace_plan/plan_lifecycle.rs`:587  · 유형: 지난 리뷰 누락  · finder: `gap1-ipc/workspace_plan/*`
- **내용**: All seven plan-mutation commands (approve, append_step, append_steps batch, remove_step, supersede_step, challenge_step_rationale, respond_to_plan_adjustment_offer) take plan_id from the wire, mutate that plan's project in the DB, then write .dive/{plan.json,plan.md,flow.mmd} into state.project_root_required() — the ambient selected project — with no plan.project_id-vs-root check (ProjectRow.path is never consulted). prepare_pre_pivot_checkpoint/maybe_create_pre_pivot_checkpoint likewise snapshot and checkpoint the ambient root's tree against the plan's session. Two triggers: (a) the already-filed usePlan stale-state races supply a project-A planId while project B is selected — approving then overwrites B's .dive/ artifacts with A's plan; (b) a pure-backend TOCTOU needing no FE bug: each project_root_required()/project_root_snapshot() call re-reads the RwLock, so a project_select completing mid-append splits a single command across two roots (checkpoint snapshot of the old root at plan_steps.rs:344, export into the new root at :385), recording an auto-pre-pivot checkpoint of B's files in B's checkpoint store attributed to A's plan session — a durable mis-attribution in the evidence chain. Fix shape: resolve the export root from the plan's own ProjectRow.path (or reject when it differs from the ambient root) once, at command entry.
- **증거**:
  ```
  let project_root = state.project_root_required()?; {     let db = state.db.lock().map_err(\|e\| e.to_string())?;     workspace_plan_service::approve_plan_and_export(db.conn(), plan_id, &project_root) // artifacts.rs:141 std::fs::write(dive_dir.join("plan.json"), ...) — no plan.project_id vs root check
  ```
- **검증**: Mechanism verified at every cited site: plan_lifecycle.rs:587-591 resolves state.project_root_required() (ambient globally-selected root, ipc/state.rs:325) and passes it with the wire plan_id to approve_plan_and_export (workspace_plan/mod.rs:10-18), whose export_plan_artifacts (workspace_plan/artifacts.rs:129-147) unconditionally writes .dive/plan.json, plan.md, flow.mmd under that root — plan.project_id is loaded (artifacts.rs:134) but never compared against the root or ProjectRow.path (which project.rs:220 proves the DB knows). The same epilogue repeats at plan_steps.rs:385 (append), :579 (r

### [P2] workspace_plan_append_steps accepts linked_criterion_ids and prd_delta parameters but the batch impl silently discards both
- **위치**: `dive/src-tauri/src/ipc/workspace_plan/plan_steps.rs`:793  · 유형: 지난 리뷰 누락  · finder: `gap1-ipc/workspace_plan/*`
- **내용**: workspace_plan_append_steps (commands.rs:271-289) accepts linked_criterion_ids and prd_delta and the FE batch wrapper (usePlanRouter.ts:172-188) types and transmits both, but workspace_plan_append_steps_impl (plan_steps.rs:723-806) consults only options.mutation_reason and passes None as prd_delta for every append_step_within_tx call (line 793); options.linked_criterion_ids is never read. The single-append and supersede paths honor both options (criterion-id override + persist_prd_delta_for_plan_mutation). The gap dates to the original S-033 batch commit (bcecb3f) and was moved verbatim by the S-066 split — no test covers the batch options contract. Latent today (the only caller, usePlanChatRouting.ts:192, sends only mutationReason), but any future batch caller supplying a PRD delta or criterion links gets them silently dropped: no error, no event, no PRD version bump. Fix: honor the options in the batch path (deciding batch semantics for a shared prd_delta/criterion list) or strip the two parameters from the command and FE wrapper.
- **증거**:
  ```
  let row = append_step_within_tx(&tx, &plan, &draft, batch_reason.as_deref(), None)?; // commands.rs builds: AppendStepOptions { mutation_reason, linked_criterion_ids: linked_criterion_ids.unwrap_or_default(), prd_delta } // impl uses only options.mutation_reason
  ```
- **검증**: All cited evidence verified verbatim: commands.rs:271-289 packs linked_criterion_ids/prd_delta into AppendStepOptions, but plan_steps.rs:723-806 reads only options.mutation_reason (line 743) and line 793 hard-codes None for prd_delta ("append_step_within_tx(&tx, &plan, &draft, batch_reason.as_deref(), None)?"); options.linked_criterion_ids appears nowhere in the batch impl. The single-append (plan_steps.rs:330-335, 376) and supersede (598-604, 644) paths honor both options, proving the asymmetry. FE wrapper usePlanRouter.ts:182-188 sends both fields; sole caller usePlanChatRouting.ts:192 passe

### [P2] looks_secret_like (sole PRD-patch secret gate) misses password:/authorization:/no-space = forms that both the deleted FE regex and the repo's own EventLog SECRET_RE catch; missed secrets reach draft rows, spec-version snapshots, and unredacted .dive/plan.json (EventLog leg is redacted; not a runtime regression)
- **위치**: `dive/src-tauri/src/ipc/workspace_plan/prd_patch.rs`:267  · 유형: 지난 리뷰 누락  · finder: `gap1-ipc/workspace_plan/*`
- **내용**: Confirmed core defect, with corrections. dive/src-tauri/src/ipc/workspace_plan/prd_patch.rs:267 looks_secret_like is the sole gate raising secret_like_text (validate_prd_patch_for_draft:185, reached from the production interview turn at prd_interview.rs:154), and its 8 fixed substrings miss forms the repo itself treats as secrets: 'password: hunter2', 'authorization: Basic ...', no-space 'token=...'/'apikey=...' all pass, while the deleted FE regex (verified at abcfbc0~1 prdPatch.ts:35-36) and — decisively — the repo's own EventLog SECRET_RE (dive/src-tauri/src/dive/event_log.rs:59-73) both catch them. A missed secret is persisted unredacted into the live draft row (prd_interview.rs:159), ProjectSpecVersion snapshots on confirm (prd_core.rs:175-207), and the verbatim .dive/plan.json export embedding project_spec/versions/live draft (artifacts.rs:141-144,170-173; only objection text is redacted) — a real leak path if the student commits .dive/. Corrections to the finder: (1) the EventLog leg of the scenario is mitigated — every append passes redact_value with the strong SECRET_RE (event_log.rs:90), so persisted PRD_PATCH_*/PRD_EDITED payloads and the pilot JSONL export redact all the cited forms; (2) 'secret=xyz' would have passed the FE regex too (value class requires 4+ chars); (3) this is NOT a runtime regression from S-061 — the FE validator had zero callers pre-deletion (that is why C7 called it dead), so the weak Rust heuristic was always the only live gate; S-061 merely deleted the stronger-but-dead reference while the baseline mislabeled the pair a 1:1 mirror. Fix is trivial: align looks_secret_like with event_log.rs SECRET_RE (or call redact-detection directly).
- **증거**:
  ```
  lower.contains("sk-")     \|\| lower.contains("api_key")     \|\| lower.contains("api-key")     \|\| lower.contains("token =")     \|\| lower.contains("token:") // FE (deleted): (?:api[_-]?key\|token\|secret\|authorization\|password)\s*[:=]\s*
  ```
- **검증**: Divergence verified: git show abcfbc0~1:dive/src/features/planning/prdPatch.ts:35-36 shows the FE pattern with password/authorization keywords and \s*[:=]\s* separators, vs prd_patch.rs:267-277 substring set that provably passes 'password: hunter2', 'authorization: Basic ...', 'token=x', 'apikey=x'. Sole-gate and sink chain verified: prd_interview.rs:154→159 (apply+persist), prd_core.rs:175-207 (version snapshot), artifacts.rs:141-144+170-173 (verbatim plan.json embedding spec/versions/draft, no redaction). Severity capped at P2 because event_log.rs:59-90 redact_value with the full-strength SE

### [P2] PRD interview turn persists patch events, the live draft, and the InterviewTurn row as separate writes on a raw connection — no transaction, so a mid-sequence failure leaves the audit ledger inconsistent with the draft
- **위치**: `dive/src-tauri/src/ipc/workspace_plan/prd_interview.rs`:124  · 유형: 지난 리뷰 누락  · finder: `gap1-ipc/workspace_plan/*`
- **내용**: The finder's mechanics are accurate; one sharpening: the earliest failure point is worse than stated — if persist_live_prd_draft itself (step 2) fails, the ledger already holds a PRD_PATCH_PROPOSED with no outcome event AND no InterviewTurn row, so the inconsistency does not even require reaching step 3/4. Fix is the established 40-lines-away pattern: open conn.transaction() before the PROPOSED append and commit after interview_turn_dao::insert (the UNSTRUCTURED/no-patch arm belongs inside the same tx).
- **증거**:
  ```
  let mut db = state.db.lock().map_err(\|e\| e.to_string())?; let conn = db.conn_mut(); // no conn.transaction() — contrast workspace_prd_save_impl (prd_core.rs:199) dive_event_log::append_to_conn(conn, None, PRD_PATCH_PROPOSED_EVENT, ...)?; persist_live_prd_draft(conn, &applied.draft)?; interview_turn_dao::insert(conn, &NewInterviewTurn { ... })?;
  ```
- **검증**: dive/src-tauri/src/ipc/workspace_plan/prd_interview.rs:124-243 runs four separately auto-committed writes on a raw conn_mut() — PRD_PATCH_PROPOSED append (:138), persist_live_prd_draft (:159, a single upsert per prd_core.rs:917), APPLIED/REJECTED append (:162/:181), interview_turn_dao::insert (:233) — each with its own `?` early-return, and the word "transaction" appears nowhere in the file. Every other multi-DAO mutating path in the module wraps in conn.transaction(): prd_core.rs:199 (workspace_prd_save_impl, EventLog appends inside the tx), plan_lifecycle.rs:349, roadmap.rs:63, plan_steps.rs

### [P2] ToolCall table has zero production writers — per-call approved/risk_level audit columns are never populated, and the export_session structured tool_call section is always empty in real usage
- **위치**: `dive/src-tauri/src/db/dao/tool_call.rs`:22  · 유형: 지난 리뷰 누락  · finder: `gap2-DAO layer + models.r`
- **내용**: All writers of the ToolCall table are test-only: every NewToolCall construction / tool_call::insert call outside db/dao/tool_call.rs is inside #[cfg(test)] modules (card_metrics.rs:73/85/145, ipc/workmap.rs:383/433) or the integration test tests/export_jsonl.rs:94; no raw INSERT INTO ToolCall exists outside the DAO (dao/tool_call.rs:25) and no JS/sidecar writer exists. The live pi_sidecar turn persists tool calls only as Message.tool_calls JSON (agent/mod.rs:1395-1414). Consequences: (1) the per-call approved/risk_level audit columns (schema.rs:67-76) are NULL-empty in every real database; (2) the production export_session IPC (lib.rs:98, invoked from useShellMenus.ts:107) reads ToolCall at export/mod.rs:379 and its structured "tool_call" record kind is always empty in real exports; (3) the `tc_count > 0` branches of the S-069 count_by_card/counts_by_session SQL (dao/tool_call.rs:50-92) and the v19 idx_tool_call_message index are exercised only by tests. Mitigating (caps severity at P2): the supervision-consent audit is not actually lost — approvals/blocks/denials are recorded in EventLog (tool_call_start/tool_call_blocked/tool_call_denied) and tool calls survive in Message.tool_calls JSON in both DB and export. Fix: either make the runtime write ToolCall rows (populating the export's structured section) or disposition the table/DAO/index as legacy with a recorded decision.
- **증거**:
  ```
  $ grep -rn "tool_call::insert\\|tool_call_dao::insert\\|tc_dao::insert" src/ dive/card_metrics.rs:71/83/143   (inside #[cfg(test)] mod tests, starts line 18) ipc/workmap.rs:381/431           (inside #[cfg(test)] mod tests, starts line 246) card_metrics.rs:7-8  "Runtime message persistence stores … in `Message.tool_calls`; older/structured paths may also populate `ToolCall`."
  ```
- **검증**: Verified read-only: grep confirms all NewToolCall/tool_call::insert sites outside the DAO are in #[cfg(test)] mods (card_metrics.rs test mod starts :18, workmap.rs :246) or tests/export_jsonl.rs; the only INSERT INTO ToolCall is dao/tool_call.rs:25. Live persistence writes only Message.tool_calls JSON (agent/mod.rs:1395-1414). Production readers exist and hit the empty table: count_by_card/counts_by_session (dao/tool_call.rs:50-92, via workmap.rs:161) and the live export_session command (lib.rs:98 → export/mod.rs:379, called from useShellMenus.ts:107), whose "tool_call" record kind is therefor

### [P2] PlanAdjustmentOffer and NewPlanAdjustmentOffer are never-referenced pub structs (born dead 2026-06-15) that the lib-crate dead_code lint cannot flag
- **위치**: `dive/src-tauri/src/db/models.rs`:486  · 유형: 지난 리뷰 누락  · finder: `gap2-DAO layer + models.r`
- **내용**: models.rs defines three offer shapes; only PlanAdjustmentOfferRow is live (db/dao/plan_mutation.rs:207 reconstruct path, workspace_plan/artifacts.rs export). PlanAdjustmentOffer (models.rs:486) is field-for-field identical to PlanAdjustmentOfferRow, and NewPlanAdjustmentOffer (models.rs:502) is Row minus created_at/responded_at — speculative read/insert shapes added in the same commit as Row (3f3bf27) and never referenced anywhere in code at any point in history (verified by full-history pickaxe; FE and integration tests touch only the Kind/Status/Response types). They are outside the wire path D-013-06 preserved (respond IPC uses PlanAdjustmentOfferResponse*/Row/Artifact) and outside round-1 C1-C8. Being pub in an rlib/staticlib/cdylib crate, the S-060-reactivated dead_code lint cannot flag them. Safe to delete both structs outright.
- **증거**:
  ```
  $ grep -rn "PlanAdjustmentOffer" src/ \| grep -v "Row\\|Kind\\|Status\\|Response\\|Offered\\|plan_adjustment_offer" workspace_plan/artifacts.rs:59:pub struct PlanAdjustmentOfferArtifact {  (separate type) db/models.rs:486:pub struct PlanAdjustmentOffer { db/models.rs:502:pub struct NewPlanAdjustmentOffer {
  ```
- **검증**: Repo-wide grep: bare `PlanAdjustmentOffer`/`NewPlanAdjustmentOffer` appear only at dive/src-tauri/src/db/models.rs:486 and :502 — zero uses in crate src, integration tests (dive/src-tauri/tests/), or FE (FE mirrors only Kind/Status unions at dive/src/features/provocation/types.ts:402-404). Full-history pickaxe (git log -G) shows the bare token was never referenced in code at any commit — born dead in 3f3bf27 alongside PlanAdjustmentOfferRow, so this is not fallout of the S-061 deletion. Not baseline: round-1 C5 names different symbols, and D-013-06's "wire types stay" rationale ("still live th

### [P2] card::reorder rewrites N card positions as N independent implicit transactions — a crash mid-loop persists duplicate positions
- **위치**: `dive/src-tauri/src/db/dao/card.rs`:211  · 유형: 지난 리뷰 누락  · finder: `gap2-DAO layer + models.r`
- **내용**: reorder validates the id set, then loops one UPDATE per card with no surrounding transaction; its only caller (ipc/workmap.rs:121 card_reorder_impl) passes a plain &Connection, so each UPDATE commits independently. The AppState mutex prevents interleaving, but an app crash, power loss, or an I/O error partway through the loop permanently persists a mixed ordering (e.g. two cards sharing position 1), and the error path performs no rollback. list_by_session's ORDER BY position, id keeps output deterministic, but the workmap card order the student arranged is silently corrupted until they happen to reorder again. Every comparable multi-write path in the cleaned code (roadmap open, append step, PRD save, checkpoint restore) wraps a transaction; this one was missed. Wrap the loop in conn.transaction() (requires the caller to pass conn_mut, matching the other IPC sites).
- **증거**:
  ```
  card.rs:211  for (idx, id) in ordered_ids.iter().enumerate() { card.rs:212      conn.execute( card.rs:213          "UPDATE Card SET position = ?, updated_at = ? WHERE id = ? AND session_id = ?", workmap.rs:121  card_dao::reorder(db.conn(), session_id, &ordered_ids)…  // no tx
  ```
- **검증**: card.rs:211-215 loops one conn.execute UPDATE per card on a shared &Connection in autocommit mode (WAL, db/mod.rs:110) — each UPDATE is its own implicit transaction — and its sole production caller card_reorder_impl (ipc/workmap.rs:121, reached from useWorkmap.ts:324 via the card_reorder command registered at lib.rs:86) passes db.conn() with no transaction. No compensating guard exists: the Card schema (db/schema.rs:31-49) has no UNIQUE(session_id, position) and idx_card_session_position (schema.rs:193) is non-unique, so a crash/power loss or mid-loop I/O error (which returns Err after earlier

### [P2] buildPlanStepExecutionPrompt seeds the composer with hardcoded Korean (mixed with English field labels) regardless of UI locale
- **위치**: `dive/src/components/product/productShellPlanStepLogic.ts`:72  · 유형: 지난 리뷰 누락  · finder: `gap3-Unread FE component `
- **내용**: The finder is accurate on every load-bearing claim. One sharpening: the prompt is mixed-language by construction — Korean directives/constraint bullets but English structural labels ("Instruction:", "Acceptance criteria:", "Execution constraints:", "Verification:") — so the seeded block is inconsistent for ko-locale users too, not only wrong for en-locale users. A fix should thread the active locale (or a t() function) into buildPlanStepExecutionPrompt and update productShellPlanStepLogic.test.ts, which currently pins the Korean strings.
- **증거**:
  ```
  productShellPlanStepLogic.ts:71-73   const lines = [     "다음 계획 Step만 실행해 주세요.",     "",     "이 내용은 DIVE의 승인된 계획에서 가져온 실행 컨텍스트입니다. 범위를 넓히지 말고 이 Step의 완료 기준만 충족하세요.", useProductShellController.ts:1070  pushChatComposerSeed(pendingPlanStepPrompt.prompt);
  ```
- **검증**: productShellPlanStepLogic.ts:71-73 and :122-125 contain the hardcoded Korean sentences exactly as quoted, and buildPlanStepExecutionPrompt(item) takes no locale/t() input. The path is live: useProductPlanStepRuntime.ts:85 builds it into pendingPlanStepPrompt, useProductShellController.ts:1070 pushes it via pushChatComposerSeed, and ChatInput.tsx:62-67 setValue(seed) puts it in the visible editable textarea. en-locale is a real default (i18n/index.ts:30-34 returns "en" for any non-ko OS locale) and en.json carries English hint/button labels ("Step context is attached automatically…" / "Insert d

### [P2] ApprovalJudgment FE component (lines 29-109) + test are dead since 345c924; kept alive by 5 type-only imports and vacuously satisfying verify-product-flow's judgment-gate check
- **위치**: `dive/src/components/workmap/ApprovalJudgment.tsx`:29  · 유형: 지난 리뷰 누락  · finder: `gap3-Unread FE component `
- **내용**: Finder's substance is correct but undercounted: five (not two/three) production files type-import from ApprovalJudgment.tsx — StepDetailSlideIn.tsx:22, useProductShellController.ts:3, features/roadmap/types.ts:2, hooks/useWorkmap.ts:4, hooks/useChatSession.ts:2 — all `import type` of ApprovalOutcome/ApprovalDecision/ApprovalDecisionWithTime (lines 5-21), which ARE live and must be relocated, not deleted. Aggravating detail the finder missed: dive/scripts/verify-product-flow.mjs:386 (verify:v4 "verification is honest" check) asserts the string "ApprovalJudgment" appears in StepDetailSlideIn — currently satisfied only by the dead type-import path string, i.e. the static wiring verifier for the judgment gate has silently drifted since 2026-06-13. The mechanical fix (move types to a types module, delete component fn + test) must also repoint that check per D-013-15 precedent, and must not touch the unrelated live Rust type crate::dive::ApprovalJudgment (ipc/cards.rs). Severity stays P2: 109+26 lines, no runtime harm, majority of the file's exports genuinely live.
- **증거**:
  ```
  $ grep -rn '<ApprovalJudgment' dive/src --include='*.tsx' \| grep -v test (no output — zero renderers) git show 345c924 -- dive/src \| grep ApprovalJudgment -import { ApprovalJudgment, type ApprovalDecision } from "../workmap/ApprovalJudgment"; +import type { ApprovalDecision } from "../workmap/ApprovalJudgment"; -                <ApprovalJudgment
  ```
- **검증**: Zero renderers outside its own test: grep '<ApprovalJudgment' in dive/src matches only ApprovalJudgment.test.tsx:13; all five non-test importers are type-only (dive/src/components/product/StepDetailSlideIn.tsx:22, useProductShellController.ts:3, features/roadmap/types.ts:2, hooks/useWorkmap.ts:4, hooks/useChatSession.ts:2); workmap/index.ts barrel does not export it and no dynamic/lazy import exists. git show 345c924 (2026-06-13) confirms the quoted hunks removing the only render from StepDetailSlideIn. Replacement judge UI exists (components/product/DecisionGate.tsx, VerificationReviewStepper

### [P2] mkdir lacks write_file's post-create verify_write_parent recheck — parent-symlink race creates directories outside the sandbox root and reports success
- **위치**: `dive/src-tauri/src/tools/mkdir.rs`:51  · 유형: 지난 리뷰 누락  · finder: `gap5-File-op tool impleme`
- **내용**: Two sharpenings to the finder's detail. (1) The window is not only a swap of an existing intermediate dir: for recursive mkdir of a not-yet-existing parent chain, resolve() has nothing to canonicalize (canonicalize_existing returns None), so planting the parent symlink any time between resolve() and create_dir_all wins — this is the exact "missing parent" gap fs_guard.rs:82-84 names, making the race no narrower than the one write_file guards against. (2) Because the operation IS directory creation, a verify_write_parent-style recheck placed after create_dir_all detects rather than prevents the outside creation — but that matches write_file's semantics (its own create_dir_all has the same residual empty-dir side effect; the recheck aborts the content write and surfaces PathDenied). The concrete defect is therefore that mkdir succeeds silently where write_file fails loudly. Fix: after create_dir_all/create_dir, re-run reject_symlink_components + canonicalize + ensure_inside_root on the created path (or call verify_write_parent on path plus a final-component check) and return PathDenied on failure, mirroring write_file.rs:46.
- **증거**:
  ```
  mkdir.rs:51  if args.recursive { tokio::fs::create_dir_all(&path).await?; } mkdir.rs:53  } else if !path.exists() { tokio::fs::create_dir(&path).await?; } // no ctx.fs.verify_write_parent(&path) — compare write_file.rs:46 which DOES call it write_file.rs:44-46  if let Some(parent)=path.parent(){ create_dir_all(parent).await?; ctx.fs.verify_write_parent(&path)?; }
  ```
- **검증**: mkdir.rs:44 resolves, then mkdir.rs:51-55 calls create_dir_all/create_dir with no post-resolve recheck, while write_file.rs:46 calls ctx.fs.verify_write_parent after its create_dir_all — the guard fs_guard.rs:82-84 documents as closing exactly the "attacker swaps a missing parent for a symlink between logical path validation and write" gap (regression-tested at fs_guard.rs:256-272 for the write path only). Mkdir is live in production (registry.rs:35). For recursive mkdir "a/b" with "a" missing at resolve time, canonicalize_existing returns None, resolve passes, and a symlink a→/outside planted

### [P2] Tracked QA evidence docs contain 19 dangling image links to purged screenshots, plus a false "screenshots preserved" claim (dd9e220/D-013-15 citation-fix pass missed them)
- **위치**: `docs/qa/conference-readiness-2026-07-10/report.md`:61  · 유형: 신규(정비 산물)  · finder: `s058-59-70-hygiene`
- **내용**: All finder claims verified exactly. Two sharpenings: (1) beyond the 12 links, report.md:244 prose asserts the 18 original screenshots are preserved at docs/qa/conference-readiness-2026-07-10/screenshots/ — affirmatively false in the public repo (directory contains only report.md), so the fix must neutralize that sentence too, not just the links; (2) the images are not merely untracked but history-purged (git log shows zero commits for any docs/qa jpg), so the links are permanently unresolvable for public readers, and .gitignore:86-89 prevents re-adding them. Fix as finder proposed: neutralize the 19 links + line 244 with the established "내부 연구 노트/스크린샷(공개 저장소 미포함)" phrasing, and apply the same to the literal 01-교수법.md path in round2-audit-findings.md:19.
- **증거**:
  ```
  report.md:61: - 관찰 근거: [09-plan-retry-blocked-english-implementation-detail.jpg](screenshots/09-...jpg) s057-go-run-log.md:39: - ![회차 1 — 3개 Step 완료](./s057-run1-rerun-3steps-complete.jpg) $ ls docs/qa/conference-readiness-2026-07-10/ → report.md (only; no screenshots/) .gitignore (dd9e220): docs/qa/**/*.jpg round2-audit-findings.md:19: ... + `docs/research/conference-poster/01-교수법.md` + ...
  ```
- **검증**: docs/qa/conference-readiness-2026-07-10/report.md (tracked) has 12 links to screenshots/*.jpg at lines 61, 92, 100, 108, 236-242 and docs/qa/011-live-qa/s057-go-run-log.md (tracked) has 7 embeds of ./s057-*.jpg at lines 39-40, 55-56, 70-71, 131, while `git ls-files 'docs/qa/**/*.jpg'` returns 0, .gitignore:86-89 (added in dd9e220) blocks the images, and the filter-repo purge left no history for them — every link 404s for a public reader. dd9e220's message claims "Dependency fixes so nothing dangles" and D-013-15 (specs/013-public-release-readiness/decisions.md) claims all citations at internal

### [P2] S-070 home-path scrub missed a pre-truncated '/Users/wily' fragment at round2-audit-findings.md:647, contradicting the D-013-09 completion record
- **위치**: `docs/qa/round2-audit-findings.md`:647  · 유형: 신규(정비 산물)  · finder: `s058-59-70-hygiene`
- **내용**: The S-070 home-path scrub (61b97ac, recorded complete in D-013-09 as "personal home paths scrubbed in the working tree (18 docs)") missed one occurrence: docs/qa/round2-audit-findings.md:647 ends with the truncated fragment "wired to the `loading` prop in /Users/wily". The line was already cut off mid-path at "/Users/wily" before the scrub (generation-time truncation in the audit doc), so the /Users/wilycastle replacement pattern never matched it. Tree-wide grep confirms this is the sole remaining personal-path residue in tracked files (all other /Users/ or C:/Users/ hits are generic placeholders). Privacy impact is trivial — the username prefix leaks nothing beyond the commit-author email deliberately left in history per D-013-09 — but the stage's completion claim is falsified, and a post-scrub `grep -r '/Users/'` verification step would have caught it. Fix: one-line edit (replace fragment with `~` or the completed `~/DIVE-2/.../RecoveryPanel.tsx` path).
- **증거**:
  ```
  round2-audit-findings.md:647: ...exposes `checkpointsLoading` (line 198), wired to the `loading` prop in /Users/wily $ git show 61b97ac~1:docs/qa/round2-audit-findings.md \| sed -n 647p \| tail -c 130 ...wired to the `loading` prop in /Users/wily   (pre-scrub line already truncated) $ git show 61b97ac -- docs/qa/round2-audit-findings.md \| grep -c '^[-+].*wilycastle' → 32
  ```
- **검증**: docs/qa/round2-audit-findings.md:647 currently ends with "wired to the `loading` prop in /Users/wily" (verified by direct read and git grep of HEAD 758d807). git show 61b97ac~1 confirms the line was already truncated at "/Users/wily" pre-scrub, so the /Users/wilycastle pattern in scrub commit 61b97ac could not match it. decisions.md:240-243 (D-013-09) records the scrub as resolved/complete ("personal home paths scrubbed in the working tree (18 docs)"), which the residue contradicts. Not BASELINE: the baseline items (public-release-code-review-2026-07-20.md:32,135 "추적 문서 15개에 개인 홈 경로") were res

### [P2] Reopen with unchanged inputs refires the supervisor eval, wipes card engagement, and — via backend session dedup — the review card never reappears
- **위치**: `dive/src/components/product/StepDetailSlideIn.tsx`:642  · 유형: 신규(정비 산물)  · finder: `s064-correctness`
- **내용**: The finder's mechanics are exact, but the outcome is slightly worse than stated: the refired eval is not just redundant — because SESSION_DEDUP (provocation_agent.rs:41, decision.rs:499-506) remembers artifact+concern+evidence_hash per session, the byte-identical re-decision is dropped as Duplicate (non-retryable, provocation_agent.rs:139-156) after the redundant LLM turn and its ledger append. The FE therefore ends with cards=[] permanently, so on close→reopen the engaged review card and its stage-3 'Review response' requirement silently disappear from the stepper (stage is skipped when no cards) rather than being re-asked. Each open/close cycle costs one wasted supervisor LLM turn plus one phantom supervision-evaluation ledger entry. Fix as the finder suggests: on close, cancel but keep the settled token (or track a lastSettledKey) so a matching key on reopen skips the restart and retains cards/engagement; also correct the :636-638 comment and add a reopen-with-unchanged-inputs test pinning eval-call count and card survival.
- **증거**:
  ```
  StepDetailSlideIn.tsx:639-644:     if (!open) {       if (supervisorEvalInflightRef.current) {         supervisorEvalInflightRef.current.cancelled = true;         supervisorEvalInflightRef.current = null;       } :669: if (inflight !== null && !inflight.cancelled && inflight.key === requestKey) return;
  ```
- **검증**: StepDetailSlideIn.tsx:639-643 nulls supervisorEvalInflightRef on close; the restart guard at :669 requires `inflight !== null`, so a reopen with a byte-identical requestKey always restarts: :679 clears provocationCards, the pruning effect (:714-720) empties engagedProvocationCardIds, and evaluateProvocationSupervisor refires — contradicting the :636-638 comment. Backend confirms the cost: provocation_agent.rs:167-204 runs a live supervisor LLM turn and appends a supervision-evaluation ledger row per attempt; and the per-session dedup (supervisor/decision.rs:499-506) drops the identical re-deci

### [P2] spec-status.md 013 stage table still shows all of S-058–S-070 as "Ready" on the public repo although every stage landed and the flip is recorded resolved (D-013-16)
- **위치**: `docs/spec-status.md`:91  · 유형: 신규(정비 산물)  · finder: `s058-59-70-hygiene`
- **내용**: The canonical, CI-hard-read status doc (docs/spec-status.md, kept public per D-013-15) tells a public reader the entire public-release round never started: lines 91-103 mark S-058–S-070 "Ready" and the section prose says only "staged", while at HEAD all 13 stages are committed and D-013-16 records the flip completed via the fresh public airmang/DIVE. This is drift, not a planned batch update — the round already edited this doc once (D-013-08 rewrote the S-048 6b follow-up to "Resolved … closed in S-063") yet left the table untouched, and prior rounds' tables (011 at lines 117-124, 010, 005) all carry Done-with-evidence rows. Fix: flip the 013 rows to Done with per-stage evidence (or link decisions.md), update the section prose and Last-updated date.
- **증거**:
  ```
  docs/spec-status.md:91: \| S-058 \| Public hygiene: ... \| Ready \| docs/spec-status.md:103: \| S-070 \| History purge ... final flip gate — strictly last \| Ready \| (contrast) docs/spec-status.md:117: \| S-050 \| ... \| Done (live QA PASS) \| git log: 758d807 docs(s-070): flip resolved via fresh repo (D-013-16) — round complete
  ```
- **검증**: docs/spec-status.md:91-103 at clean HEAD (98c19db) lists all thirteen S-058–S-070 rows as "Ready" while git log shows every stage committed and decisions.md D-013-16 records the public flip resolved (links already repointed to the public repo in d7a5eaf). Not baseline: the review doc has zero "spec-status" mentions and the 013 table postdates it (a84a833); no D-013 entry defers table updates. The "pending round-close pass" defense fails because the same doc WAS updated mid-round — D-013-08 flipped the S-048 6b follow-up section (spec-status.md:177-185) to "Resolved … closed in S-063" — and D-0

### [P2] PiSidecarSmokeResult (pub, lint-exempt) left un-gated in production builds after 0651bd3 cfg(test)-gated its entire producer/consumer chain
- **위치**: `dive/src-tauri/src/pi_sidecar.rs`:90  · 유형: 신규(정비 산물)  · finder: `s060-rust-deadcode`
- **내용**: Commit 0651bd3 gated run_codex_smoke and its SMOKE_* constants behind #[cfg(test)] but left the return type PiSidecarSmokeResult (with Serialize/Deserialize derives that existed only for the deleted IPC command) un-gated at pi_sidecar.rs:89-97. It is a field-for-field duplicate of the live PiSidecarTurnResult and has zero non-test references; being pub in the dive_lib rlib, the re-enabled dead_code lint cannot flag it. Fix: add #[cfg(test)] to the struct (or reuse PiSidecarTurnResult as run_codex_smoke's return type) to complete the S-060 disposition.
- **증거**:
  ```
  pi_sidecar.rs:89-90: #[derive(Debug, Clone, Serialize, Deserialize, PartialEq, Eq)] pub struct PiSidecarSmokeResult {  Only refs: pi_sidecar.rs:181 (cfg(test) run_codex_smoke sig), :329 (its body). Zero other callers repo-wide.
  ```
- **검증**: pi_sidecar.rs:89-97 — PiSidecarSmokeResult is un-gated while every producer/consumer (DEFAULT_MODEL/SMOKE_MARKER/SMOKE_PROMPT at :58-63, run_codex_smoke at :175-176) was #[cfg(test)]-gated by 0651bd3; only 3 repo-wide refs, all inside the cfg(test) fn (:181, :329), no TS/bindings/main.rs use. lib.rs:13 `pub mod pi_sidecar` in the dive_lib rlib makes the pub struct externally reachable, so the freshly re-enabled dead_code lint is silent on it — the exact lint-exempt category the C1 sweep hand-hunted. Baseline C4 covers only the deregistered pi_sidecar_codex_smoke command and no D-013 decision m

### [P2] Post-success refresh rejection inside runUserAction composites is mislabeled as the mutation failing ('Could not save step' for a saved step; connect-classified alert for a successful provider select) — but retries cannot create duplicate steps (backend duplicate guard)
- **위치**: `dive/src/components/product/PlanAddStepPanel.tsx`:633  · 유형: 신규(정비 산물)  · finder: `s062-silent-failures`
- **내용**: The S-062 fix (6d9812f) wraps whole composites in runUserAction, so a rejection from a post-success refresh is mislabeled as the mutation failing. Add-step path: PlanAddStepPanel.tsx:633 wraps onAppendStep -> PlanDashboardPanel.handleAppendStep -> usePlan.appendStep, which after a successful workspace_plan_append_step invoke awaits refresh() (internally caught, usePlan.ts:169-181) then refreshPrdStatus() (usePlan.ts:188-198, NO catch). If workspace_prd_status Errs there (poisoned lock, DB I/O, or a corrupted LiveProjectSpecDraft row — prd_dao::get_draft parses draft JSON and is the one call the append path does not exercise), the student sees toast 'Could not save step' (단계 저장 실패) with the prd-status error as description, and the form stays populated, for a step that WAS saved. Settings sibling: settings.tsx:289-295 wraps selectProvider + loadAll in one runUserAction; loadAll rethrows (project-session.ts), so a post-select refresh failure renders a connect-classified alert for a selection that succeeded. CORRECTION to the finder's harm claim: retries CANNOT append duplicate step rows — workspace_plan_append_step_with_options_impl calls find_duplicate_step before insert (plan_steps.rs:365, impl :1108-1135), which normalize-matches title/summary/instruction_seed against active steps, so the retained form's identical title makes every retry Err('step already exists: …'). Worst case is two misleading toasts, not plan corruption. Trigger is rare (the same DB just served the append, and refresh() already swallowed one occurrence of the identical error). Fix shape stands: run the mutation alone inside runUserAction and treat post-success refreshes as non-fatal (their hooks already own error state).
- **증거**:
  ```
  usePlan.ts:339-341   await refresh(); await refreshPrdStatus(); return row;   // inside appendStep usePlan.ts:193-195   await api.invoke<WorkspacePrdStatusWire>("workspace_prd_status", ...) // no catch PlanAddStepPanel.tsx:645   title: t("toast.plan_add_step_failed"),   // "Could not save step" PlanAddStepPanel.tsx:650   if (!result.ok) return;   // form retained -> resubmit appends again
  ```
- **검증**: Mechanism CONFIRMED in code: usePlan.ts:339-341 chains uncaught refreshPrdStatus() after the successful append invoke (refresh() at :169-181 catches, refreshPrdStatus at :188-198 does not); PlanDashboardPanel.tsx:323-326 wires it into the runUserAction composite at PlanAddStepPanel.tsx:633-650 whose onError shows toast.plan_add_step_failed = 'Could not save step' (en.json:1742) and retains the form; workspace_prd_status_impl (prd_core.rs:103-108) can Err, including via prd_dao::get_draft JSON parsing (db/dao/prd.rs:127-138) which the append path never calls. settings.tsx:289-295 + project-sess

### [P2] S-063 dotenv-read rule hard-blocks reads of non-secret .env.example/.env.sample templates (and *.env.ts sources) via run_process and terminal_script
- **위치**: `dive/src-tauri/src/tools/guard.rs`:177  · 유형: 신규(정비 산물)  · finder: `fresh-rust-agent-tools`
- **내용**: The S-063 promotion of the dotenv-read rule into the shared classify_bash_command (guard.rs:177-180) kept the pre-existing trailing boundary `\.env(?:\b\|[.\s])`, which matches `.env` followed by a dot — so every `.env.*` name, including the conventional non-secret templates `.env.example`/`.env.sample`/`.env.template` and source files like `app.env.ts`, trips a hard "dotenv read" block. This contradicts the rule's own comment claiming the boundary excludes non-dotenv names. Sharpening vs. the finder: the `.env.example` over-match itself is NOT new — the pre-S-063 terminal-script rule (8eba36e, old guard.rs:320) had the identical boundary, so `cat .env.example` was already blocked on terminal_script. What S-063 newly added is (a) run_process coverage (previously no dotenv rule in classify_bash_command — all these reads were allowed) and (b) nine additional readers (head/tail/less/more/sed/awk/grep/source/`.`) on both tools. The surfaced BlockReason.rule is "dotenv read" (not literally "credential exposure" — that was the old terminal-script category). The block is applied in validate() before run and is non-overridable by user approval (run_process.rs:74-76). Consequence bounded: read_file has no dotenv gating so the agent can still read the template (verified: zero dotenv references in read_file.rs/fs_guard.rs), but a beginner-guided flow gets a misleading secret-leak block on the one env file whose purpose is to be read. Fix: exclude `.example`/`.sample`/`.template` suffixes from the dotenv match (and optionally require the `.env` component to be the file name's start, killing the `app.env.ts` class).
- **증거**:
  ```
  // guard.rs "dotenv read" (177-180), now in classify_bash_command Regex::new(r"(?i)(?:\b(?:cat\|...\|grep\|source)\b\|...)[^;&\|\r\n]*(?:^\|[/\\])?\.env(?:\b\|[.\s])") // live: BLOCK 'cat .env.example'  BLOCK 'grep KEY .env.example'  BLOCK 'cat config/.env.example' // pre-S063 run_process.rs called classify_bash_command WITHOUT any dotenv rule -> was allowed
  ```
- **검증**: Reproduced exactly with the shipped regex from guard.rs:178: 'cat .env.example', 'grep KEY .env.example', 'cat config/.env.example', 'cat .env.sample', and 'cat app.env.ts' all match the "dotenv read" rule, contradicting the rule's own comment (guard.rs:175-176) that the trailing boundary keeps non-dotenv names from matching. git show 8eba36e confirms pre-S-063 classify_bash_command had no dotenv rule (run_process allowed all of these) and the terminal-script rule covered only cat/type/get-content/gc. The block is a hard, non-approvable validate() error (run_process.rs:74-76; guard.rs header: 

### [P2] qrcode.react remains a production dependency after S-060 (0651bd3) deleted its sole consumer provisioning-demo.tsx
- **위치**: `dive/package.json`:64  · 유형: 신규(정비 산물)  · finder: `ci-deps-build`
- **내용**: Commit 7628efe added qrcode.react ^4.2.0 together with its only importer, dive/src/pages/provisioning-demo.tsx (QRCodeSVG for teacher child-key QR distribution). S-060's dead-code sweep (0651bd3, recorded as D-013-05) deleted that page and its /?demo=provisioning route but left the package.json dependencies entry. No runtime impact — the module is never imported so Vite never bundles it — but it is a dead production dep that inflates install weight and the pnpm-audit surface, and depcheck/external reviewers of the newly public repo will flag it right after a dedicated dead-code round. Fix: remove the entry (revival path is git history, same rationale D-013-05 uses for the deleted engine) and regenerate pnpm-lock.yaml.
- **증거**:
  ```
  $ grep -rni "qrcode" src/ scripts/ index.html pi-sidecar/src  -> (no output) $ git log --oneline -S "qrcode.react" -- dive/src 0651bd3 refactor(s-060): rust dead-code sweep — ... dead IPC surface 7628efe feat(auth): OpenRouter provisioning keys + teacher demo UI (task 3-5) package.json:64  "qrcode.react": "^4.2.0",  (in "dependencies")
  ```
- **검증**: dive/package.json:64 has "qrcode.react": "^4.2.0" in production dependencies; a full-tree grep of dive/ (src, scripts, index.html, pi-sidecar, src-tauri; excluding node_modules/dist) yields no other hit. git -S shows only two commits: 7628efe added the dep with its sole consumer dive/src/pages/provisioning-demo.tsx (import { QRCodeSVG } from "qrcode.react"), and 0651bd3 (S-060, per D-013-05) deleted that file (248 lines) and its demo route without touching package.json; no later commit touches dive/package.json. Not baseline: docs/qa/public-release-code-review-2026-07-20.md:134 lists only axe-

### [P2] README docs-map (line 141) and 연구 배경 (line 148) still advertise docs/research/ as holding the KACE2026 research outputs that D-013-15 internalized — a miss of the dd9e220 neutralization pass that only fixed line 22
- **위치**: `README.md`:141  · 유형: 신규(정비 산물)  · finder: `secrets-tree-hygiene`
- **내용**: After the D-013-15 minimal-public docs cut, docs/research/ tracks exactly one file (conference-poster/references.md, a bibliography). Commit dd9e220 neutralized only README line 22; the docs-map row (line 141, "연구 산출물 (KACE2026 발표)") and the 연구 배경 section (line 148, "관련 산출물은 … docs/research/에 정리되어 있습니다") still promise the internalized research material to public readers, contradicting D-013-15's own "internal notes (not in the public repository)" standard. Same-species drift: line 137's "캐논 스펙 (001–010)" omits approved canon 011 and 013. Fix is safe — the CI-hard-read README markers (release-gate lines) are unaffected.
- **증거**:
  ```
  README.md:141: \| [`docs/research/`](./docs/research/) \| 연구 산출물 (KACE2026 발표) \| README.md:148: 관련 산출물은 2026 한국컴퓨터교육학회(KACE2026) 발표를 목표로 [`docs/research/`](./docs/research/)에 정리되어 있습니다. $ git ls-files docs/research/ → docs/research/conference-poster/references.md (only file)
  ```
- **검증**: README.md:141 ("연구 산출물 (KACE2026 발표)") and README.md:148 ("관련 산출물은 … docs/research/에 정리되어 있습니다") both verified verbatim, while `git ls-files docs/research/` returns only conference-poster/references.md — the KACE2026 papers/poster/deck are untracked per D-013-15. `git show dd9e220` proves the D-013-15 README neutralization touched only line 22, so 141/148 are a fix-pass miss that contradicts D-013-15's own recorded claim ("README … citations … were fixed to neutral phrasing"). Baseline check: the 2026-07-20 review's only README item is A3 (version drift, different) and no D-013 entry defers th

### [P2] CHANGELOG [Unreleased] falsely claims 'no pending unreleased changes' with 28 commits (incl. user-visible behavior changes) unreleased since rc.9; link defs stale at rc.4
- **위치**: `CHANGELOG.md`:7  · 유형: 신규(정비 산물)  · finder: `secrets-tree-hygiene`
- **내용**: The [Unreleased] section (CHANGELOG.md:7) affirmatively claims no pending unreleased changes while 28 commits sit on main past the v1.0.0-rc.9 tag (S-058–S-071 plus two pre-round fixes, e533a2b being itself a user-visible PRD fix), including the curl/wget plain-GET egress block (D-013-08), silent-failure surfacing, deleted OpenRouter-provisioning IPC, an FE/Rust correctness bug pack, and restored macOS CI distribution. Same-file staleness: the link-definition block ends at rc.4 so headings rc.5–rc.9 render as dead bracket labels, and the [Unreleased] compare link (line 369) spans five releases (v1.0.0-rc.4...HEAD). The file was edited twice during the 013 round (dd9e220, d7a5eaf) without correcting either. Fix at or before the public flip: either write the pending-changes entry (or the rc.10 entry) and extend the link defs through rc.9, or replace the '없음' line with an honest 'changes since rc.9 pending release' pointer.
- **증거**:
  ```
  CHANGELOG.md:5-7: ## [Unreleased]\n\n_대기 중인 미출시 변경 사항 없음._ $ git log --oneline e533a2b..HEAD \| wc -l → 26 CHANGELOG.md:369: [Unreleased]: https://github.com/airmang/DIVE/compare/v1.0.0-rc.4...HEAD
  ```
- **검증**: CHANGELOG.md:5-7 affirmatively states '대기 중인 미출시 변경 사항 없음' while `git log v1.0.0-rc.9..HEAD` shows 28 unreleased commits on main, including user-visible behavior changes (D-013-08 curl/wget plain-GET block at 4fa6699, silent-failure toasts 6d9812f, S-064 bug pack ffb35ac, deleted OpenRouter-provisioning IPC 0651bd3, macOS CI distribution d3ddfab). Link-def staleness also verified: CHANGELOG.md:369 pins [Unreleased] to compare/v1.0.0-rc.4...HEAD and the def block stops at rc.4 while headings rc.5–rc.9 (lines 9/76/93/111/144) have no defs; the file was edited twice this round (dd9e220, d7a5eaf) 

### [P2] Public repo self-contradicts on macOS as a distribution target: README/dive-README say Windows-only, S-071 build.yml says 'macOS distribution' restored, release.yml still publishes Windows-only
- **위치**: `README.md`:9  · 유형: 신규(정비 산물)  · finder: `secrets-tree-hygiene`
- **내용**: Commit d3ddfab (S-071) added a build-macos job whose workflow comment and commit message call it restored 'macOS distribution' (Tier 1 unsigned .app.zip, 'users unzip and drag to Applications'). But (a) root README keeps the Windows-only platform badge (line 9), Windows-only 설치 section, and line 113 ('배포 대상은 Windows(x64·ARM64) NSIS 인스톨러입니다'); (b) dive/README.md frames macOS as local-QA-only and says CI builds only Windows installers; and (c) release.yml — the workflow that actually attaches release assets — still ships only the two Windows NSIS .exe files, leaving the mac .app.zip as a 14-day CI artifact. The fix must pick one story and reconcile all three surfaces: either macOS is a distribution target (update README badge/설치/113, dive/README build section, and attach the .app.zip in release.yml) or it is not (soften the build.yml 'distribution' comment). If a README update is queued inside the in-flight S-071 stage, this item guards against it being dropped at the public flip.
- **증거**:
  ```
  README.md:9: [![platform](https://img.shields.io/badge/platform-Windows%20x64%20%7C%20ARM64-...)] README.md:113: 배포 대상은 Windows(x64·ARM64) NSIS 인스톨러입니다. git show d3ddfab: 'Restore macOS distribution ... builds the darwin Pi sidecar SEA and a Tauri .app, zips it as the primary artifact'
  ```
- **검증**: Not baseline: docs/qa/public-release-code-review-2026-07-20.md touches README only for version drift (A3) and specs/013-public-release-readiness/decisions.md contains no macOS/platform/distribution decision; d3ddfab (07-21) postdates the review (07-20). Evidence verified at HEAD: README.md:9 badge 'Windows x64 \| ARM64', README.md:113 '배포 대상은 Windows(x64·ARM64) NSIS 인스톨러입니다', Windows-only 설치 section, and dive/README.md:125 'CI builds Windows x64 and ARM64 installers' — while .github/workflows/build.yml:199-206 (S-071) self-describes as 'macOS distribution (Tier 1)' producing a user-installable

### [P2] run_capped's wall-clock timeout bounds only the read loop, not child exit — a child that closes stdout+stderr while still running wedges the supervised turn for its full runtime, defeating the advertised timeout (real regression, but approval-gated + availability-only + narrow trigger ⇒ P2, not P1)
- **위치**: `dive/src-tauri/src/tools/process_exec.rs`:178  · 유형: 회귀(정비가 유발)  · finder: `s063-security`
- **내용**: The S-063 refactor (commit 4fa6699) split the old `timeout(t, cmd.output())` into `timeout(t, read_loop)` (process_exec.rs:173) followed by an untimed `child.wait().await` (process_exec.rs:178, in the Ok(Ok(())) arm). read_loop (145-171) exits on `while !(out_cap.done && err_cap.done)`, and `done` is set on Ok(0)=EOF (149,159). A still-running child that closes its own stdout/stderr fds delivers EOF immediately, so read_loop returns Ok(()) at once while the child lives on; child.wait() then blocks with no timeout, and kill_on_drop(true) cannot fire because the child is awaited, not dropped. I proved the OS behavior directly: `exec 1>&- 2>&-; sleep 3` yields both pipes at EOF at 0.00s and wait() returning at 3.01s. Reachable via run_terminal_script (posix) `exec 1>&- 2>&-; sleep 300`, which matches no classify_bash_command rule and no TERMINAL_SCRIPT_EXTRA_RULES entry (verified all regexes: the `&` in `1>&-`/`2>&-` is not a trailing background `&`; no `/`, nohup, curl, rm, env, or dotenv), so it is approved and executed and wedges the turn regardless of the 60/120s timeout. No outer timeout rescues it (agent/mod.rs:1059 awaits tool.run with no wrapper; no tokio::time::timeout in agent/mod.rs). The module doc comment (124-125) still falsely claims the whole wait is bounded.</parameter> <parameter name="corrected_detail">The S-063 refactor (commit 4fa6699) split the old `timeout(t, cmd.output())` into `timeout(t, read_loop)` (process_exec.rs:173) followed by an untimed `child.wait().await` (process_exec.rs:178, in the Ok(Ok(())) arm). read_loop (145-171) exits on `while !(out_cap.done && err_cap.done)`, and `done` is set on Ok(0)=EOF (149,159). A still-running child that closes its own stdout/stderr fds delivers EOF immediately, so read_loop returns Ok(()) at once while the child lives on; child.wait() then blocks with no timeout, and kill_on_drop(true) cannot fire because the child is awaited, not dropped. I proved the OS behavior directly: `exec 1>&- 2>&-; sleep 3` yields both pipes at EOF at 0.00s and wait() returning at 3.01s. Reachable via run_terminal_script (posix) `exec 1>&- 2>&-; sleep 300`, which matches no classify_bash_command rule and no TERMINAL_SCRIPT_EXTRA_RULES entry (verified all regexes: the `&` in `1>&-`/`2>&-` is not a trailing background `&`; no `/`, nohup, curl, rm, env, or dotenv), so it is approved and executed and wedges the turn regardless of the 60/120s timeout. No outer timeout rescues it (agent/mod.rs:1059 awaits tool.run with no wrapper; no tokio::time::timeout in agent/mod.rs). The module doc comment (124-125) still falsely claims the whole wait is bounded.
- **증거**:
  ```
  // process_exec.rs 146:  while !(out_cap.done && err_cap.done) {   // ends on EOF, not on exit 173:  match tokio::time::timeout(timeout, read_loop).await { 174:      Ok(Ok(())) => { 178:          let status = child.wait().await...   // <-- OUTSIDE the timeout // OLD (e533a2b run_process.rs): timeout(Duration::from_secs(timeout), cmd.output())
  ```
- **검증**: tools/process_exec.rs:146 read_loop ends on EOF (`done` set at Ok(0), lines 149/159); tools/process_exec.rs:173 timeout wraps only read_loop; tools/process_exec.rs:178 `child.wait().await` is untimed inside the Ok(Ok(())) arm — kill_on_drop can't fire on an awaited (undropped) child. Guard trace: guard.rs:396 assess_terminal_script runs classify_bash_command then TERMINAL_SCRIPT_EXTRA_RULES (guard.rs:308-390); `exec 1>&- 2>&-; sleep 300` matches none (background rule requires `&` before end/newline; path rules require `/`). agent/mod.rs:1059 dispatches tool.run with no surrounding timeout. Reg


---
## 기각(REFUTED) — 검증에서 살아남지 못함

- `dive/src-tauri/src/tools/guard.rs` — env-dump rule over-blocks the legitimate `env VAR=x cmd` run-with-env idiom
  - 기각 근거: Three decisive facts kill the finding. (1) "New behavior from the S-063 fix" is wrong: the pre-S-063 terminal-script rule (git show 4fa6699^ guard.rs:315, `(?:env\|printenv\|set)(?:\s\|$)`) already blocked `env FOO=bar node app.js` — verified by regex replay (old rule matches: true); S-063 merely promoted the rule to run_process, which is the baseline review's own prescribed F1 remediation ("run_p
- `dive/src-tauri/src/dive/supervisor/decision.rs` — p1_provoke_gate can never pass for SupervisorEvent::AiClaimedDone, making the accepted ai_claimed_done event a guaranteed provoke_false path — contradicting the fallback's own doc comment
  - 기각 근거: The gate behavior is the explicitly-specified canonical design, not a defect. specs/002-provocation-supervisor-agent/spec.md:124 (FR-005) mandates "`ai_claimed_done` only records the assistant completion claim as an evidence ref; it MUST NOT render a card on its own. A review card is rendered only at `verify_entered`"; research.md:49 records "render at both ai_claimed_done and verify_entered" as a

---

## 부록 A — 검증 미완(세션 한도/서버오류로 adjudication 실패, 생존목록 제외)

이 5건은 finder가 제기했으나 적대 검증 에이전트가 API 오류로 판정을 못 내려 생존 목록에서 빠졌다. 루트가 스팟체크한 범위만 병기한다. 후속 재검증 대상.

- `fresh-rust-agent-tools/wrapper-dashc-egress-bypass` — run_process curl/wget 래퍼 우회. **루트 스팟체크**: `validate_command_no_shell`(shell 직접 차단) + `classify_bash_command`(curl 앵커 차단) + `detect_wrapper_shell_bypass`(EXEC_WRAPPERS argv 검사)의 3중 방어로 `sh -c curl`·`env bash -c`·`timeout curl`·`xargs curl`은 모두 차단됨. 다만 EXEC_WRAPPERS 미포함 실행자(예: `find . -exec curl … {} \;`)류는 앵커·래퍼 양쪽을 비껴갈 여지 — 미확정, 재현 필요.
- `gap5/readfile-searchfiles-bypass-s063-dotenv-block` — **루트 확인: 실재(P2)**. `read_file`은 `ctx.fs.resolve_read`(경로 격리)만 하고 `.env` 내용 가드가 없어, bash `cat .env`를 막은 S-063 dotenv 규칙과 비대칭. 감독 승인 게이트 하에 있고 프로젝트 파일 읽기는 에이전트 본연 기능이라 P2.
- `fresh-rust-dive-core/coach-unsafe-action-gate-porous` — verification_coach unsafe-action 게이트 다공성. 미검증.
- `ci-deps-build/asset-protocol-home-scope-preview-exfil` — tauri asset-protocol 스코프가 홈까지 열려 preview exfil 가능성. 미검증.
- `gap3/interview-draft-cross-project-bleed` — 인터뷰 초안이 프로젝트 간 누출. 미검증(자매 항목 `stale-architecture-proposals-across-projects`는 확정 P2).

## 부록 B — 권장 처리 순서

### 0단계 — 공개 즉시 차단(P0, 지금 라이브 노출)
1. **`.omc`/`.sisyphus`/`.wily` 히스토리 퍼지**: 이 3개 디렉터리는 세 차례 filter-repo 퍼지 경로 목록에 **한 번도 포함된 적이 없어** `origin/main`과 rc.2~rc.9 전 태그에서 266개 오브젝트가 공개 서빙 중(repo는 현재 PUBLIC). `.omc/project-memory.json`엔 오너 홈경로, `.omc/specs/`엔 KACE 인터뷰 플레이북, `.wily/`엔 엔지니어링 감사 핸드오프. D-013-14/15/16의 "0 omc/omx reachable" 판정은 파일명 기준이라 이 디렉터리를 못 잡았다. → 4번째 filter-repo 패스에 `.omc .omx .sisyphus .wily` 추가 + 태그 재작성 + force-push, 그리고 D-013 계열과 동일하게 refs/pull/* 캐시 뷰 처리.
   - (verify-product-flow ENOENT P0는 리뷰 중 `98c19db`로 이미 해소 — decisions.md의 "all hard-reads fixed" 기록만 이 누락건 반영해 정정.)

### 1단계 — 로직 회귀·correctness(P1)
- verify:v4 타우톨로지 회귀(`readAll` concat이 합성-엣지 보증 상실) — includesAll을 primary-파일 한정 import/call 검사로 강화.
- S-064 패널 상시마운트 파생 회귀 2건(닫힌 코치 패널 LLM 호출·원장 기록 / 증거 latch가 리비전 라운드 넘어 잔존).
- supersede/remove 의존자 가드 비대칭, 세션삭제 좀비 매핑, 체크포인트 복원 동시성 미보호, 스텝 간 관찰 텍스트 누출, 미드스트림 재접속 시 어시스턴트 출력 소실.

### 2단계 — 위생·문서(P2, 공개 직결·수정 쉬움)
- THIRD-PARTY-NOTICES 누락(IBM Plex Sans KR + Node 런타임 + pi SDK 2종), QA문서 죽은 스크린샷 링크 19개, README/CHANGELOG/spec-status 드리프트, 홈경로 잔재 1건, tauri 템플릿 에셋·죽은 .gitkeep.

### 3단계 — 나머지 P2(코드 품질·백로그)
- 데드코드 잔여(등록만 된 IPC 7개, 리스너 없는 이벤트 5개, dead struct들), 로케일 하드코딩(ko-KR) 다수, 스토어 stale-response 가드 누락 다수, ESLint .mjs 무커버리지, 트랜잭션 누락(카드 reorder·인터뷰 턴), mkdir TOCTOU 등.
