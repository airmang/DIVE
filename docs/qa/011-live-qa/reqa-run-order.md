# 011 재QA 지시서 — 1차 라이브 QA FAIL 항목 재검증 (2026-07-11)

1차 결과(`tier1-run-log.md`) 기준, 두 핫픽스 반영 재빌드 앱으로 **차단됐던
저니만** 재실행한다. PASS였던 저니 A(모델 선택/실행), E(오류 표면화),
F3(보관), 수동 provenance는 재실행 불필요.

대상 앱: `dive/src-tauri/target/release/bundle/macos/DIVE.app`
(빌드 시각이 이 지시서 커밋 이후인지 확인.)

적용된 핫픽스:
1. `68102db` — 계획 게이트가 criterion ID("AC-001") 참조를 PRD 본문으로
   해석 (1차 FAIL의 직접 원인).
2. 후속 커밋 — PRD 인터뷰 프롬프트에 단일 JSON 응답 계약 명시
   (구조화 no_json 미수렴 원인).

증거: 스크린샷은 `screenshots/`에 `reqa-` 접두사, 결과는
`tier1-run-log.md` 하단에 "재QA (2026-07-11)" 섹션으로 추가. 커밋 금지.

## 재저니 1 — 계획 게이트 (1차 저니 B 재실행, 최우선)

기존 `qa-tier1-checklist` 프로젝트 재사용 (보관 해제 상태), 모델은
Sonnet 5 유지.

1. `이 PRD로 계획 만들기` 실행.
2. **PASS: 계획 초안 검토 화면 도달** (AC-001 차단 없음).
3. 도달하면 계획을 승인하고 → **재저니 3으로 계속**.

## 재저니 2 — PRD 구조화 (1차 저니 C 재실행)

새 프로젝트 `qa-reqa-interview`.

1. 1차와 같은 상세 한국어 답변(사용자·범위·비범위·제약·완료 기준 포함) 1회
   전송.
2. **PASS: 필드에 반영**(하이라이트). 실패 시 다시 구조화 1회로 수렴하는지.
3. 반영됐다면 일부 필드를 수동 수정 → 확정 카드가 **혼합 문구**(학생 작성
   필드 열거)인지 확인 (1차 SKIP이었던 혼합 provenance).

## 재저니 3 — 실행·검증·성공 후 관찰 (1차 저니 D + F1/F2)

재저니 1에서 승인된 계획으로:

1. step-1 세션 시작 → **F1**: "세션을 시작해 대화를 시작하세요"가 아니라
   세션 시작 안내 문구가 뜨는지. (스크린샷)
2. step 실행 → 파일 편집 권한 승인 → 구현 → 미리보기 확인.
3. **F2**: 검토에서 기준 2개 이상이면 관찰 기록 폼의 체크박스 +
   "모두에 적용" 버튼으로 관찰 1회 기록 → 여러 기준 충족 확인.
4. **D**: 성공 후 90초 무조작 관찰 → **stall timeout 오류 0회**.

## 판정

세 재저니 모두 PASS면 1차 티어 + S-056 라이브 검증 완료 → 루트가 wily
마감 및 S-057 GO 판정 준비(clean app-data 3연속 E2E)로 진행한다.
