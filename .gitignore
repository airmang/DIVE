# Ralph 루프 로그 (사용자별 환경, 커밋 대상 아님)
ralph_logs/

# OMC (oh-my-claudecode) 작업 디렉토리 — 사용자별 핸드오프/플랜/세션 상태
.omc/

# 로컬 QA 산출물 (Computer Use / release smoke 재현용, 커밋 대상 아님)
qa-sandbox/
.qa-backups/

# Playwright/검증 스크립트 산출물 (스크린샷 등, 커밋 대상 아님)
dive/test-results/

# Wily 실행 세션 로그는 로드맵 감사 문서와 분리한다.
.wily/sessions/

# Superpowers 브레인스토밍 비주얼 컴패니언 산출물 (사용자별, 커밋 대상 아님)
.superpowers/

# OS
.DS_Store
Thumbs.db

# 에디터
.vscode/*
!.vscode/extensions.json
.idea
*.sw?

# 환경 변수·시크릿
.env
.env.local
.env.*.local

# Harness work-orders / wily plans (generated, regenerable from tasks.md)
.harness/

# Internal conference/research working material — never public.
# The whole conference-poster tree is local research (unpublished paper
# drafts, pilot protocol, pedagogy design, advisory docs, stage plans, QA
# evidence); only the public reference list is tracked.
docs/research/conference-poster/*
docs/research/conference-poster/*/
!docs/research/conference-poster/references.md
docs/release-readiness/

# Agent/tooling scratch directories (사용자별, 커밋 대상 아님)
.orca/
.omc/
.omx/
.playwright-mcp/
.pnpm-store/

# KACE 발표 덱 편집 파이프라인 (별도 repo에서 빌드, 로컬 산출물)
decks/

# 루트 작업 산출물
fig1-preview.png
